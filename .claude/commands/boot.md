---
description: 작업 세션 부팅 — 최신 거시(코스피·나스닥 등) 라이브 조회 + 시세·뉴스 신선도 확인, 미수집 시 자동 수집 후 통합 보고.
allowed-tools: Read, Bash, Skill
---

작업 세션 부팅을 수행한다. **고정 순서**:

1. **콘솔 현재 날짜 확인** — `TZ=Asia/Seoul date '+%Y-%m-%d %H:%M (%a)'` 실행. **이 콘솔 날짜를 기준으로 판단**(모델 추정 날짜 신뢰 금지).

2. **`collect-macro` 스킬** — 최신 거시지표(코스피·코스닥·나스닥·S&P·SOX·환율·금리·유가)**만** 라이브 조회·적재 + **실시간 오버레이(`python -m trading.regime`, EXEC-7)**. 그 외 데이터(전종목·공시·뉴스)는 수집하지 않는다.
   - EOD 지표는 공개 시차(+1영업일)가 있다 — **보고에 as_of를 반드시 병기**하고, 코스피·코스닥은 실시간(토스) 값·당일 등락·레짐 판정을 함께 표기한다(7/14 오독 재발 방지: 폭락 다음날 아침에 폭락 전 값이 백드롭으로 나가면 안 된다).

3. **`work-boot` 스킬** — 그 외는 **DB에서 읽되, 미수집이면 부팅 중 자동 수집한다**: 전종목 시세 신선도 + **오늘자 뉴스 신선도** + **자동 사이클 점검(`drill.py --audit` — cron 16슬롯 가동 중)** + 오늘 스크리너 후보 + history.
   - **시세가 최근 거래일보다 뒤처지면 → `collect` 스킬을 즉시 실행**해 갭을 메운다(질문하지 않는다). 스크리너는 수집 후 계산.
   - **오늘 뉴스 미적재 + 키 있으면 → `collect-news` 스킬을 즉시 실행.** 키 없으면 수집 불가 — 키 안내만(COLLECT-3, 흉내내기 금지).
   - 사이클 WARN/FAIL이면 ⚠️ 잡명+사유 보고(라운드 재트리거는 하지 않는다 — 점검·보고만).
   - 공시 등 그 외 무거운 수집은 부팅에서 하지 않는다.

수집을 포함해 모든 단계가 끝난 뒤, 마지막에 통합 보고:
```
## 부팅 — <콘솔 날짜>
### 시장 (실시간 + EOD 백드롭)
- **실시간(토스): 코스피/코스닥 현재가·당일 % + 레짐(NORMAL/CAUTION/RISK_OFF/UNKNOWN)**
- EOD(as_of 병기): 나스닥/S&P/SOX/환율/금리/유가 — 코스피·코스닥 EOD값이 실시간과 크게 다르면 실시간 우선
### 자동 사이클 (drill --audit)
- 16슬롯 판정 요약              (WARN/FAIL 시 ⚠️ 잡명+사유, 게이트웨이 생존 포함)
### 데이터 신선도
- 시세 DB 최신일 vs 최근 거래일       (부팅 중 자동 수집했으면 '수집함' 명시)
- 뉴스: 오늘자 적재 여부(N건/없음)    (부팅 중 자동 수집했으면 '수집함' 명시, 키 없으면 키 안내)
### 오늘 감시 풀 (approved — 실제 오늘 대상)
- `python -m trading.approve --pool` 다이제스트(종목명·손절/경고/익절·만료)
### 스크리너 후보 (EOD 참고용)
- 상위 N + 섹터 태그. **as_of가 최근 거래일보다 이전(공개대기)이면 "전일 미반영 — 참고용" 라벨 필수.** 레짐이 CAUTION/RISK_OFF이거나 전일 지수 급변이면 이 순위는 무효에 가깝다고 명시
### 마지막 / 다음 작업
```
