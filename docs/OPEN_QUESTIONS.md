# OPEN QUESTIONS

설계서의 모호함·미결정 사항을 임의 해석하지 않고 여기에 기록한다 (CLAUDE.md rule #1).
형식: 상태(🟢결정 / 🟡회색지대·확인필요 / 🔴미결정) · 항목 · 맥락 · 잠정 처리.

> **2026-08-26 설계서 v0.3 비준(PIVOT-1·7 — 장기 사이클·가치 투자 피벗):** 스윙 전제 항목
> (SEL-1~4, KIS-RT-1, R5-1, R7-1, EXEC-1~11, NEWS 촉매 계열)의 🟡 잔여·해소책은 **일괄 폐기** —
> 역사 기록으로만 유지하며 후속 작업 대상이 아니다. 존속: SCHED-*, COLLECT-*(뉴스는 축소 존치
> 범위 재검토), CAL-1~3(캘린더 자체), STRUCT-1, SECT-1, INFRA-*, PIVOT-*.

---

## 스케줄러

### 🟢 SCHED-1 — 스케줄링은 openclaw 기능만 사용
- **결정(2026-06-08, 운영자 지시):** 스케줄링은 **openclaw `cron` 또는 `heartbeat` 전용**. 자체 Python 스케줄러·system cron·launchd 등 외부 스케줄러 금지.
- 결과: 설계서/M2의 "`src/scheduler/` 자체 시장시간 스케줄러" 지시는 폐기. 시장 캘린더(KRX 휴장일·US DST)·장중 게이팅은 **각 cron 잡이 호출하는 스크립트 내부 가드**로 구현(openclaw 공식 권장 패턴).
- cron: `--at/--every/--cron`+`--tz Asia/Seoul`, 잡별 `--model/--tools`, isolated 세션, SQLite 영속. heartbeat: 주기 에이전트 턴(기본 30m), `activeHours`(tz 윈도우) → 장중 09:00–15:30 연속 감시에 사용.

### 🟢 SCHED-2 — 순수-코드 경로의 LLM 트리거 (수용됨)
- **맥락:** 설계서는 R0(수집)/R1(검증)/R5.5(아침 선택기)/이벤트 감시기를 **순수 코드(LLM 금지)**로 규정(환각 차단). 그러나 openclaw cron·heartbeat 페이로드는 `--message`(에이전트 턴)/`--system-event`뿐 — **순수 셸 실행 모드(`--command`)가 없음**. 즉 openclaw-only에서는 이 경로들도 에이전트 턴이 트리거.
- **잠정 처리:** cron/heartbeat 에이전트 턴은 `--tools exec --light-context` + 결정론적 디스패치 프롬프트로 **스크립트만 실행**한다. LLM은 트리거 전용 — FactRecord 등 데이터·판단 로직엔 일절 개입하지 않는다(숫자를 보지도 처리하지도 않음). 환각 차단 취지는 유지.
- **수용(2026-06-08):** 운영자가 "LLM=트리거 only, 데이터·판단엔 미개입" 해석을 수용. 디스패치 스크립트가 모든 실제 로직을 결정론적으로 수행.

### 🟢 SCHED-3 — claude 라운드의 모델 라우팅 (결정됨)
- **결정(2026-06-08, STRUCT-1=B의 귀결):** **Python 두뇌가 LLM 호출을 직접 오케스트레이션.** R2/R3=OpenAI API(Python SDK), R4/R5/R7=`claude -p` 서브프로세스. openclaw provider 라우팅은 라운드에 미사용(openclaw는 스케줄러+알림 채널 역할).
- 인증: `claude -p`는 로컬 Claude Code 인증 사용(운영자 보유). R2/R3는 Python `.env`의 `OPENAI_API_KEY` 필요(openclaw의 OpenAI OAuth는 재사용 안 함). 모델명은 `.env` 주입(하드코딩 금지).

---

## 수집 (R0)

### 🟢 COLLECT-1 — R0 수집을 LLM이 수행 (CLAUDE.md 금지 #2 override)
- **결정(2026-06-08, 운영자):** R0 수집을 순수 코드가 아니라 **LLM 에이전트가 수행**한다(운영: openclaw cron 하루 2회·gpt-5.5 / 수동: `/collect`·`/boot`). CLAUDE.md 절대금지 #2(R0=순수코드)를 **R0 한정**으로 폐기.
- **R1(검증)·R5.5(선택기)·이벤트 감시기는 순수 코드 유지**(환각 차단 취지는 거기 남음).
- **환각 가드(필수):** 모든 수치·사실은 실제 소스 응답에서만. `source`·`as_of`·`fetched_at`(KST) 필수, 기억 기반 수치 금지, 검증 불가 → `verified=0`/`UNVERIFIED`(추측 금지).

### 🟢 COLLECT-2 — 데이터 소스 확정 (2026-06-08 조사)
| 데이터 | 소스 | 비용/계좌 | 실시간성 |
|---|---|---|---|
| 금리·환율 | 한국은행 **ECOS** Open API | 무료(상업가능) | 일별 |
| 국내지수(KOSPI/KOSDAQ)·종목 EOD | **공공데이터포털 금융위** / **KRX OPEN API** | 무료 | EOD(+1영업일·비상업) |
| 해외지수(SOX·S&P·NASDAQ)·유가(WTI·Brent) | **FRED**(세인트루이스 연은) | 무료 키 | 일별/EOD |
| 국내 종목 실시간 시세·호가 | **KIS Developers**(+`migusdn/KIS_MCP_Server` MCP, trading OFF) | 계좌(모의 지원), 초당 20건 | 실시간 근접 |
- **KIS 해외선물/해외지수는 제외:** SOX 미확인 + 해외선물 계좌·권한 별도 → 해외 매크로는 FRED로.
- 정확한 엔드포인트·FRED 시리즈ID·KIS TR_ID·필드는 **각 공식 문서에서 확정**(추측 금지).
- **남은 갭:** ~~① 국내 투자자별 매매동향(수급) KIS TR 확인~~(**2026-06-11 해소** — 아래) · ② NXT 프리/애프터(기존 🔴) · ③ 실시간 필요 범위(라운드=EOD 충분, 장중 감시만 KIS 실시간).
- **갭① 해소(2026-06-11) — 수급은 KIS TR로 확정** (공식 저장소 `koreainvestment/open-trading-api` + 실호출 관측 검증, KRX 정보데이터시스템 직접 접근 불필요):
  - 종목별 투자자매매동향(일별): `GET /uapi/domestic-stock/v1/quotations/investor-trade-by-stock-daily` TR `FHPTJ04160001` — output2가 일별 ~30거래일, `{frgn,prsn,orgn}_ntby_qty`(주)/`..._ntby_tr_pbmn`(**백만원** — 수량×주가 대조로 단위 검증).
  - 시장별 투자자매매동향(일별): `GET /uapi/domestic-stock/v1/quotations/inquire-investor-daily-by-market` TR `FHPTJ04040000` — KOSPI=(업종 0001, KSP)/KOSDAQ=(업종 1001, KSQ) 파라미터 조합 실호출 확정.
  - 토큰: `POST /oauth2/tokenP`(24h 유효, 6시간 내 재발급=동일 토큰+알림톡) → 파일 캐시 필수(`.runtime/kis/token.json`).
  - 구현: `collectors/kis.py`(조회 전용 — 주문 TR 금지 유지) + `collectors/flows.py`(`data/flows.sqlite` append-only) + `collect-flows` 라운드(daily-eod 체인 내 best-effort) + FactPack `flows` 섹션(R3 수급 grounding).
  - 장중 잠정(시세성): `GET /uapi/domestic-stock/v1/quotations/inquire-investor-time-by-market` TR `FHPTJ04030000` — 파라미터 (999, S001)=KOSPI/(999, S101)=KOSDAQ은 장중 조합 프로브 관측 확정(그 외 0/오류, HTS [0403] 코스피·코스닥 화면과 정합). 표시 전용·적재 금지(응답에 날짜 필드 부재). **잠정 단위 🟢 해소(2026-06-11 15:38 마감 직후 교차검증):** 단위는 일별과 동일 **백만원** 확인. 단 **시세성 피드는 신뢰 불가 실증**(2026-06-11): KRX 정보데이터시스템 장중 CSV와 대조 결과 절대값 5~30배 과소(13:38 외인: KIS 잠정 -1,564억 vs KRX -29,529억) — 동시호가 미반영 수준이 아니라 커버리지 자체가 부분. 방향 판단에도 쓰지 말 것(6/11 '동시호가 외인 매도' 오판 사례 → `history/trading/events/2026-06-11-intraday-flow-reversal.md`). 장중 수급 정답 소스는 KRX 정보데이터시스템 수동 CSV(자동화는 비공식 스크래핑이라 보류).

### 🟢 COLLECT-3 — 수집 하네스 (LLM 독자 웹서치 금지) · **뉴스는 COLLECT-4로 부분 개정(2026-06-09)**
- **결정(2026-06-08, 운영자):** LLM은 **승인된 소스 어댑터/도구만** 호출한다. **독자 판단의 웹서치(WebSearch/WebFetch) 금지** — 지침이 아니라 구조로 차단.
- 구현: 수집 커맨드/스킬의 `allowed-tools`에서 웹서치 제거 → 승인 도구(소스 어댑터: ECOS/공공데이터/KRX/FRED REST, KIS MCP)만 노출.
- 데이터 fetch는 **결정론적 어댑터 계층**이 수행, LLM은 트리거·기록만. 소스 실패·키 미연결 시 `UNVERIFIED`/`blocked`로 기록하고 **다른 소스나 웹서치로 임의 대체 금지**.
- 어댑터 미구현 구간은 수집을 **건너뛰고 보고**(웹서치 우회 금지).
- **적용범위(2026-06-09 명확화):** 시세·거시·공시 클러스터에 **그대로 엄격 유지**. **뉴스 클러스터만 COLLECT-4로 예외.**

### 🟢 COLLECT-4 — 뉴스 수집은 웹서치 허용 (2026-06-09, 운영자 결정 / COLLECT-3 부분 개정)
- **배경:** 뉴스는 후보 촉매 + **해외 거시·테마**(SOX·연준·글로벌 반도체 등 한국 증시 동인) 둘 다 필요.
  네이버 검색 API는 해외뉴스 약하고, 다수 언론사 RSS 직결은 유지보수 불가 → **웹서치 메타검색이 현실적 유일안.**
- **결정:** **뉴스 클러스터에 한해** COLLECT-3의 '독자 웹서치 금지'를 해제. 단 '구조 차단'을 **출처 가드로 대체**(환각구멍 방지):
  - 뉴스 검색은 **단일 뉴스 search 어댑터**(SearXNG 셀프호스트 또는 검색 API) 경로로만. 임의 도구 난사 금지.
  - 모든 `NewsRecord`: 실제 **URL·발행처·published_at(KST)·fetched_at·source 필수**. 다운스트림이 인용하는 사실은 **실제 fetch된 기사에 귀속** — 기억 기반 헤드라인/출처/수치 날조 금지.
  - **dedup**(URL·제목 정규화) + 출처신뢰 랭킹. 저신뢰·검증불가 → `UNVERIFIED`(드롭 후 날조 금지).
  - LLM은 **쿼리 확장·다양화·필터/그룹핑 가능**(리콜 확보에 필요한 능력). 단 사실은 fetch된 결과만.
  - 검색 실패·백엔드 다운 → `blocked` 보고(빈 결과 지어내지 않음).
- **미결(별도 결정 → PROPOSALS):** ① 검색 백엔드(SearXNG vs 검색 API) ② 저비용 프로바이더(MiniMax 등) 채택 여부 ③ 스코프(후보 촉매 / 일반 트렌드 발굴 / 둘 다).

### 🟡 NEWS-L2 — L2 섹터·테마 쿼리의 활성 서브테마 큐레이션 출처 (PROPOSALS P-4 §3)
- **배경:** P-4 3계층 쿼리플랜의 **L2(섹터·테마)**를 구현하며 — 현재 grounded 베이스는 **26 `Sector` 라벨**(`domains.SECTORS`에서 결정론 파생, `_sector_query`)로 확정. 종목명 검색에 안 잡히는 테마-광범위 촉매(예: 젠슨황 방한→AI 연관주)를 섹터 키워드로 포착.
- **회색지대:** 프로포절은 "26섹터 + **활성테마**(반도체→HBM·CXL, AI→온디바이스 등 **서브테마**)"를 말하나, 서브테마 리스트는 **큐레이션 출처가 없으면 임의확장 = 환각구멍**(절대금지 #1 정신). 현재는 **서브테마 미수집**(26 섹터 라벨만) — 보수적 스텁.
- **잠정 처리:** L2 = 26 섹터 라벨로만 가동. 서브테마 확장은 출처 확정(섹터 메타에 큐레이션 필드 추가 / 운영자 제공 리스트 / R7 학습 산출) 후 `build_query_plan`에 주입. **그 전까지 LLM·기억으로 서브테마 지어내지 않는다.**

### 🟢 NEWS-R2 — R2 촉매 스코어러 아키텍처 (결정: 단일 호출·claude -p 런타임, 멀티에이전트 아님)
- **질문(PROPOSALS P-4 잔여미결②):** `catalyst_strength`·분류 스코어러를 **단일 호출** vs **멀티에이전트(앙상블)**? + 어느 모델/프로바이더?
- **결정(2026-06-09, 운영자):**
  - **(아키)** **단일 스키마-강제 호출(배치).** 멀티에이전트 아님.
  - **(프로바이더)** **`claude -p` 서브프로세스(로컬 Claude Code 인증, 외부 OpenAI 키 불필요).** 설계서 §2의 "GPT-5.5"는 **프레임워크로 서빙할 모델의 플레이스홀더**였지 OpenAI 확정이 아니었음(운영자 명확화). 확정 아키(SCHED-3)가 이미 "Python 두뇌가 **직접 호출**, openclaw provider 라우팅 미사용"이라 R4/R5/R7과 **같은 claude -p 경로 재사용** → 두 번째 프로바이더·키·과금 0. 모델/프로바이더는 **`Scorer`/`LLMClient` 인터페이스 뒤 추상화** → .env 스위치로 드롭인 교체.
- **단일호출 근거:**
  1. 설계서 §2가 "뉴스 분류·요약·이벤트 추출"을 **고빈도·저단가·스키마 강제** 레이어로 둠 — R2는 **구조적 추출**이지 심층 토론·합성이 아님.
  2. R2는 06:30/16:30 **광범위 배치** 처리 → 항목별 멀티에이전트는 비용·지연 폭발.
  3. **검증·다양성은 R4가 담당**(perspective-diverse, **선별만** — 고강도·`single_stock`). R2 멀티화는 R4와 중복 → 프로포절 §4 "전수 검증 금지" 위배.
  4. `catalyst_strength` 신뢰는 **R7이 사후 캘리브레이션**(폐루프).
  5. 멀티에이전트 패턴은 설계상 **R3(페르소나 ×3)·R4(적대)** 전용.
- **claude -p 실측 봉투(2026-06-09):** `--output-format json` → `{is_error, subtype, result, total_cost_usd, modelUsage, …}`. 텍스트=`.result`, 성공=`is_error==false && subtype=="success"`, 모델=`--model`. 비용 유의(콜당 캐시생성 포함 ~$0.06) → R2는 **저단가 모델 .env 주입**(하드코딩 금지) + 배치로 콜 수 억제.
- **귀결(구현):** 배치 = scope 레이어별(L1 후보별 / L2 섹터별 / L3 거시), 각 배치 → **1회 호출**이 기사를 **이벤트로 클러스터링**(같은 촉매 다기사 = 1 EventRecord + `novelty` 디스카운트) + 촉매필드 채움. 환각가드: `affected`/`evidence`는 fetch된 기사에만 귀속(미검증=UNVERIFIED, 종목 박제 금지).
- **안전선:** R2 산출은 EventRecord→R3 ThesisRecord grounding으로만. **R1 게이트·R5.5(매매 발동)에 LLM 점수 주입 금지**(절대금지 #2).

---

## 시장 캘린더

### 🟢 CAL-1 — KRX 휴장일 데이터 출처 (음력·대체공휴일·임시휴장) — **2026-07-11 종결**
- **맥락:** `market_calendar` 가드(SCHED-1)가 거래일을 판정한다. 월-일 고정 공휴일(신정·삼일절·근로자의날·어린이날·현충일·광복절·개천절·한글날·성탄절·연말휴장 12/31)은 코드(`_FIXED_CLOSED_MD`)에 확정. **음력 공휴일(설날·추석·석가탄신일)·대체공휴일·임시휴장·제헌절은 추측 금지** — 연도별 명시 날짜가 필요했다.
- **관측(2026-07-11, 연속성 가드):** `--check`가 달력 미등록 휴장 **9일**을 드러냈다. 승인 소스(data.go.kr)가 **정상 응답 + 무자료**로 답한 날들(소스 장애는 `CollectError` raise이므로 빈 응답=휴장 관측).
- **결정(운영자 2026-07-11):** 관측 9일을 **공식 출처로 대조해 전부 확인**하고 `krx_holidays.json`에 등록. 대조 결과 관측치와 공지가 **9/9 일치**(추측 수정 없음). 미관측 미래 휴장일 5일도 같은 공지에서 확인해 함께 등록:
  | 등록분 | 근거 |
  |---|---|
  | 2025-10-06·07·08 | 추석 연휴 + 대체공휴일(10/8) |
  | 2026-02-16·17·18 | 설 연휴 |
  | 2026-03-02 / 05-25 / 08-17 / 10-05 | 대체공휴일 4회(삼일절·부처님오신날·광복절·개천절) |
  | 2026-06-03 | 제8회 전국동시지방선거 — **KRX 발표** |
  | **2026-07-17** | **제헌절(2026년 공휴일 재지정) — KRX 발표. 관측 불가(미래)라 이번 대조가 아니었으면 놓쳤을 휴장일** |
  | 2026-09-24·25 | 추석 연휴(9/26은 토) — **대체공휴일 없음**(설·추석은 일요일 중복 시에만 적용) |
- **출처:** KRX 발표 보도([한국경제 2026-05-20](https://www.hankyung.com/article/2026052094456) — 6/3·7/17 전 시장 휴장) · [2026 휴장일·대체공휴일 4회 목록](https://www.dpi1004.com/10769) · [2026 추석 대체공휴일 미적용](https://www.hankyung.com/article/2025063085857) · 관공서의 공휴일에 관한 규정.
- **제헌절은 연도별 항목으로 둔다** — `_FIXED_CLOSED_MD`(연도 무관)에 (7,17)을 넣으면 2025년 이전 7/17 거래일을 휴장으로 오판한다(2025-07-17은 정상 거래일). 테스트로 박제.
- **재발 방지(침묵 금지):** 파일에 `covered_through`(현재 `2026-12-31`) — 이 범위를 넘긴 판정은 `MarketCalendar.is_covered()=False`이고 `--check`가 ⚠️ 만료 경고를 낸다. **2027년 KRX 공지가 나오면 파일 갱신**(연 1회 운영 작업). 관측 휴장일도 `--check`가 등록/미등록으로 분리 보고 → 미등록분만 갱신 대상.
- **자동화(미채택):** data.go.kr 특일정보 API 어댑터는 승인 소스 미확정이라 구현하지 않았다(엔드포인트 추측 금지). 연 1회 수동 갱신으로 충분 — 필요해지면 PROPOSALS로.

### 🟢 CAL-3 — KRX 거래시간 연장(애프터마켓)과 §5 "장중"의 범위 — **2026-07-11 결정**
- **확인된 사실(2026-06-19 KRX 일정 재조정):**
  - **애프터마켓 16:00–20:00 — 2026-09-14 시행**([한국일보](https://www.hankookilbo.com/news/article/A2026062310130002438), [뉴스핌](https://www.newspim.com/news/view/20260619000738)).
  - **프리마켓 07:00–07:50 — 2027년 말로 재연기**([파이낸셜뉴스](https://www.fnnews.com/news/202606191644561782), [비즈워치](https://news.bizwatch.co.kr/article/market/2026/06/19/0028)). 당초 6/29 → 9/14 연기([노컷뉴스](https://www.nocutnews.co.kr/news/6486442))에서 프리마켓만 재차 밀렸다.
  - **정규장 09:00–15:30 불변.**
- **결정(운영자 2026-07-11):** **§5 "장중" = 정규장 ∪ 애프터마켓.** 애프터마켓은 실거래가 도는 시간이므로 LLM 라운드·주문 설계를 모두 휴면시킨다(보수적 해석).
- **구현:**
  - `in_after_market`/`in_extended_session` + `AFTER_MARKET_EFFECTIVE=2026-09-14`(시행일 이전 날짜엔 창 없음 — 과거 리플레이가 열려 있던 시장을 휴장으로 오판하지 않게). `require_llm_rounds_allowed`·`require_market_closed`가 확장 창을 본다. `in_krx_session`(정규장)은 **불변** — 체결·수급·arm-check가 쓰는 축이라 분리.
  - **가드 배선(신규):** §5 휴면은 그동안 **슬롯 배치로만** 지켜졌다(`require_llm_rounds_allowed`가 정의만 되고 호출부 0). `trading.run` 디스패치가 LLM 라운드 진입 전 가드를 호출하도록 배선 — 장중이면 `rc=3`(정상 스킵, P1 알림 없음). 수동 CLI(`python -m trading.score_news`)는 이 경로를 안 타므로 CAL-2대로 우회 유지.
  - **pm 체인 재배치**(애프터마켓 마감 후, 20:00 벽에 붙여 압축): score-pm 16:32→**20:02** · verify-pm 16:45→**20:15** · reason-pm 16:55→**20:32** · synth-pm 20:30→**21:05** · report-pm(저녁 결재) 21:00→**21:30**(운영자 요청 — 30분 순연으로 최소화). 실측 소요(R2 ~64분·R4 ~23분·R3 ~25분·R5 ~4분) 기준. R4/R3는 기존과 동일하게 R2 완료를 기다리지 않고 그때까지 적재된 이벤트를 처리하며, **R5만 R3 논제를 DB로 받으므로 R3 종료(~20:57) 뒤**에 둔다. news-pm(16:20)은 **순수 어댑터 수집이라 유지**(마감 뉴스를 제때 받는다).
    ⚠️ **여유가 얇다**: R2가 실측(64분)보다 크게 늘면 R5가 그 시점까지 적재된 논제만 쓴다. 9/14 이후 실운영에서 체인 종료 시각을 관측해 필요하면 보고를 뒤로 미룬다.
  - 매니페스트 회귀 테스트: LLM 슬롯이 휴면 창 안으로 되돌아오면 `test_cron_llm_slots_outside_dormant_window`가 깨진다.
- **SEL-1 영향:** **해소되지 않는다.** KRX 프리마켓이 2027 말로 밀려 프리마켓 흐름변수는 여전히 NXT 의존(🔴 유지).
- **잔여(9/14 전후 확인):** 애프터마켓 체결이 **data.go.kr 일별시세의 종가·거래량 정의**에 포함되는지 미확인 — 포함되면 스크리너 거래대금·모멘텀의 의미가 바뀐다. 시행 후 실데이터로 대조(추측 금지).

### 🟢 CAL-2 — 장중 LLM 가드의 적용 범위 (cron 한정 vs 수동 실행 포함) — **2026-06-12 결정**
- **맥락:** 설계서 §5 "장중(09:00–15:30) LLM 라운드 전면 휴면". 가드(`require_llm_rounds_allowed`/
  `require_market_closed`)는 구현됨. 항상 강제하면 **운영자 수동 실행**(장중 재합성 등)도 차단된다.
- **결정(운영자 2026-06-12):** **cron 자동 경로는 가드 강제, 수동 CLI는 `--force`로 우회 허용.**
  R5(synth_playbooks)에 `--force` 플래그 — 장중 가드(`require_market_closed`)를 수동 한정으로 우회.
  정합 근거: ① R5 입력은 EOD라 장중 실시간 가격에 휩쓸리지 않음 ② P-7로 집행이 아침 승인으로
  분리돼 산출은 draft → 다음 거래일 arm-check 승인(충동 집행 차단 유지). 장중 강제 시 경고 출력.
  cron 디스패치(`run._synth_playbooks`)는 force 미전달 → 가드 유지(장중 자동 실행 없음).
- **확장:** 다른 LLM 라운드(R2~R4·R7)도 수동 백필 필요 시 동일 `--force` 패턴 적용 가능(현재는 R5만).

---

## 라운드 파이프라인

### 🟡 SEL-1 — R5.5 흐름 관측치 소스 (NXT 프리마켓 어댑터 부재) — **2026-06-12 부분 해소**
- **맥락:** R5.5 선택기 입력(갭·프리마켓 거래량·미국 마감·환율 개장가) 중 프리마켓 계열은 NXT 데이터(🔴 외부 의존) 필요. 어댑터 미구현.
- **2026-07-11(CAL-3 조사):** KRX 자체 프리마켓(07:00–07:50)이 **2027년 말로 재연기** → 이 경로로는 해소되지 않는다. NXT 의존 유지(🔴).
- **2026-06-12 부분 해소(P-6 arm-check):** `flowsnap.build_snapshot`이 KIS 실시간(KIS-RT-1)으로
  **체결강도(execution_strength)·전일고가 회복(prev_day_high_reclaim)·호가 불균형(orderbook_imbalance)**
  3종을 실데이터로 채움(2026-06-12 장중 실호출 검증). **프리마켓 거래량(premkt_volume_ratio)·갭(gap_pct)은
  여전히 NXT 의존 🔴** — "관측치 없음"으로 정직 표기. 잔여 NXT 변수는 주입 파일 보충 경로 유지.
- **R5 조건 제약(2026-06-12, 운영자 결정):** R5가 미수집 변수(premkt 등)로 조건을 짜면 매일
  '관측치 없음'이라 그 플레이북은 영영 발동 불가(실증: 6/11 3종 다 premkt가 첫 조건이라 미발동).
  → `flowsnap.OBSERVABLE_FLOW_VARS`(KIS 자동 수집 3종) 단일 소스를 R5 프롬프트에 주입해 arm/abort/
  confirmation 조건을 **관측 가능 변수로만** 짜게 제약. NXT 어댑터가 생기면 OBSERVABLE에 추가만 하면
  R5 프롬프트가 자동 반영. (기존 premkt 포함 플레이북은 재합성 전까지 잔존 — 다음 R5부터 정리.)
- **잠정 처리(보수):** 선택기는 순수 함수, 관측치는 KIS 실시간 + `.runtime/flow/<YYYYMMDD>.json` 주입 파일.
  **둘 다 없음=빈 스냅샷=전 플레이북 비활성(비거래)** — 관측치 추측 금지. 조건식 문법은 `<op><숫자>`·
  `==true/==false`(SEL-2), 시각·문자열은 평가 불가=미충족.
- **해소책:** NXT 소스 확정(🔴) 후 premkt 계열을 OBSERVABLE_FLOW_VARS·스냅샷에 추가 — R5·선택기 변경 불필요.

### 🟢 KIS-RT-1 — KIS 실시간 시세성 TR (P-6 arm-check) — **2026-06-12 관측 확정**
- **맥락:** arm-check가 9~10시 흐름 관측치를 KIS 실시간으로 채우려면 현재가·체결강도·호가 TR 필요.
- **관측 확정(2026-06-12 장중 실호출):**
  - 주식현재가 체결 `GET /uapi/domestic-stock/v1/quotations/inquire-ccnl` TR `FHKST01010300` —
    output[0]에 `stck_prpr`(현재가)·`tday_rltv`(당일 체결강도, 100 기준) 관측. (현재가 시세 `inquire-price`
    `FHKST01010100`엔 체결강도 필드 없음 — 미사용 확인.)
  - 주식현재가 호가 `GET /uapi/domestic-stock/v1/quotations/inquire-asking-price-exp-ccn` TR `FHKST01010200` —
    output1에 `total_bidp_rsqn`·`total_askp_rsqn`(매수/매도 총호가잔량) → imbalance=(bid−ask)/(bid+ask) 관측.
- **상태:** 🟢 확정. `collectors/kis.py`(quote_ccnl·quote_asking_price), 결측·비수치는 None=관측치 없음(추측 금지).

### 🟢 SEL-3 — R5.5 cron(select-am) 날짜 라벨 어긋남 — **2026-06-12 해소**
- **맥락(2026-06-12 발견):** R5(synth-pm, 밤 20:30)는 `pb.<당일>`로 생성하는데 R5.5(select-am,
  다음날 아침)·기존 arm-check는 `playbooks_for_day(<다음날>)`로 조회 → 하루 어긋나 전일 밤 승인분을
  못 찾았다(실증: 6/12 아침이 `pb.20260611`을 못 봄). 자동 arm이 사실상 빈 풀로 동작했을 가능성.
- **해소(P-7):** arm-check·`select_playbooks` 둘 다 `PlaybookStore.active_playbooks`(status=approved
  + TTL(time_stop_days 거래일), 날짜 라벨 비의존)로 통일. select_playbooks의 흐름 소스도 arm-check와
  같은 `flowsnap.build_snapshot`(KIS 실시간 + 주입 파일)으로 일원화, `load_snapshot` 제거.
  검증: `test_runner_arms_across_date_label_mismatch`(6/11 승인 → 6/12 아침 arm), 실DB 6/12 실행 정상.

### 🟢 SEL-2 — R5 흐름변수 boolean 조건과 selector 숫자 문법 불일치 — **2026-06-12 해소**
- **맥락:** R5가 `prev_day_high_reclaim ==true`처럼 boolean 흐름변수 조건을 산출하는데, selector(`engine._COND`)는
  `<op><숫자>`만 평가 → `==true`는 평가 불가=미충족으로 빠졌다(arm-check 실호출에서 노출). 현재가가
  전고를 회복(1.0)해도 발동 판정에 반영되지 않았다.
- **해소(택1 — 둘 다 적용):** ① selector `engine.eval_condition`에 `==true/==false`(대소문자 무관) 평가 추가
  (`_BOOL`, 관측치 1.0=참/0.0=거짓 — flowsnap 인코딩과 정합). ② R5 프롬프트에 조건식 문법 가이드
  명시(연속 변수=`<op><숫자>`, boolean 변수=`==true/==false`, 시각·문자열 금지). explain도 boolean을
  '= 예/아니오'로 표기. 기존 6/11 데이터(`==true`)가 재산출 없이 즉시 평가됨(실증). 시각(`09:30`)
  등은 여전히 평가 불가.
- 검증: `test_boolean_condition_eval`·`test_boolean_arm_condition_activates`(end-to-end), 실DB arm-check
  에서 `prev_day_high_reclaim = 예(true) → 충족(O)` 확인.

### 🟢 SEL-4 — 가격 회복 조건은 계획 명시·등급형으로, 전일 고가 기준선은 진짜 T-1로 (운영자 결정 2026-07-14)
- **맥락(같은 날 뉴파워 사건에서 적발된 결함 2건):**
  ① 7/13 초안 7건 전부가 `prev_day_high_reclaim==true`를 arm 조건으로 달았다 — 코드 강제는 아니나
  프롬프트 기본값 유도("보통 prev_day_high_reclaim") + 가격 레벨 변수가 이진 하나뿐인 어휘 빈곤으로
  사실상 고정 가드였다. 운영자: **"고정 적용이 아니라 전날 계획이 완전/일부 회복 등을 조건으로
  걸었을 때만 적용되어야."**
  ② "전일 고가"의 실체가 EOD DB 최신 행 = 장중엔 항상 **T-2 고가**(국내 EOD +1영업일 공개).
  뉴파워 실측: 진짜 전일(7/13) 고가 10,400(익절 10,000 위 — 발동 불가 구조)인데 T-2(7/10) 고가
  9,870 기준으로 09:54 오발동.
- **결정·해소(당일 구현, fix/flow-graded-reclaim):**
  - **기본 조건 주입 폐지**: R5 프롬프트에서 기본값 유도 제거 + "조건은 분석에서 도출됐을 때만,
    습관적·기본 조건 금지" 명문화. confirmation_condition 미제공 시 기본 주입 대신 **플레이북 폐기**.
  - **등급형 변수 신설**: `prev_day_high_recovery`(연속, 현재가/전일 고가, 1.0=도달) — 완전 회복
    `>=1.0`, 일부 회복은 분석이 정당화하는 임계(예: `>=0.97`)로 계획이 명시. 이진 reclaim은 유지(호환).
  - **트리거≈타깃 설계 금지**를 R5 절대 규칙에 추가(잔여 R:R<1 플레이북 산출 금지 — 집행 가드와 이중).
  - **기준선 교정**: KIS 일자별 시세 TR `FHKST01010400` 실호출 관측 확정(2026-07-14,
    `stck_bsop_date/oprc/hgpr/lwpr/clpr`) → `daily_prices()` 신설, flowsnap `_prev_day_high`가
    1차로 사용(당일 행 스킵 → 직전 거래일 행). 폴백 EOD DB는 **최신 적재일=직전 거래일일 때만**
    신뢰, 불일치면 미관측(보수 미충족) — 낡은 기준 판정 금지.
- 검증: `test_prev_high_prefers_kis_daily_over_stale_db`·`test_prev_high_stale_db_yields_no_observation`·
  `test_confirmation_condition_missing_discards_playbook`·`test_graded_recovery_is_flow_variable` +
  실DB arm_check에서 뉴파워 reclaim이 10,400 기준 미충족(0)으로 전환 확인.
- **2차 개정(2026-07-14 밤, 운영자 "여전히 전고점 기준이 너무 강해"):** 21:05 첫 산출에서 4건 중
  3건이 여전히 `reclaim==true`(-5% 급락일에 완전 회복 = 사실상 미발동 계획) → ① **이진 reclaim을
  신규 계획에서 폐지**(arm/abort에 쓰면 폐기 — 완전 회복도 `recovery>=1.0`으로 명시) ② R5에
  **당일 고가·종가 이격 주입**(`synth._intraday_price_lines`, KIS 일자별 결정론) ③ 임계 산정 가이드
  (이격 -3% 이내만 완전 회복 현실적, 그 외 일부 회복 임계·타 신호). 기존 풀 초안의 reclaim 평가는
  유지(관측·선택기 불변). confirmation 트랜치 키는 v1 미집행이라 폐지 대상 아님.
- **잔여(🟡):** 가드 일괄 감사(2026-07-14)의 나머지 항목(A5·C2·C3·D1)은 별도 수정 대기.

### 🟡 R7-1 — R7 채점·레짐의 입력 데이터 갭
- **맥락:** 설계서 §3 R7 채점은 "트리거 발동 후 시계 내 방향 일치"·운영자 준수율, 레짐은 시초 1시간 변동성·개인 강도 프록시(신용잔고·예탁금·레버리지 ETF)를 요구. 현재 ①흐름 데이터 부재로 트리거 발동 감지 불가 ②집행 데이터 부재(KIS 미구현)로 준수율 측정 불가 ③레짐 입력(금융투자협회 신용잔고 등, 부록 A) 수집기 부재.
- **잠정 처리(보수, notes에 명시):** 방향 채점은 **트리거 무관**(as_of 익영업일→horizon 종가), 준수율 미측정 명시, 레짐은 전종목 |등락률| 중앙값 비율 프록시만. 미성숙·결측 논제는 채점하지 않음(부분 채점 금지).
- **해소책:** 흐름 스냅샷 어댑터(SEL-1과 공유) + KIS 체결 어댑터 + 금융투자협회 수집기.

### 🟡 R5-1 — "생존 논제" 판정 (논제 레벨 적대 라운드 부재)
- **맥락:** 설계서 §3 R4는 **논제(ThesisRecord) 살해 시도**, R5 입력은 "생존 논제". 그러나 현 구현의 R4(verify-catalysts)는 **이벤트(촉매) 검증**이다 — 논제 레벨 적대 라운드는 미구현. R3 논제를 무엇으로 거를지 미결정.
- **잠정 처리(보수적):** R5는 최근 논제 중 `direction != flat` 전부를 입력하되, 촉매 이벤트의 R4 검증 상태([생존]/[기각])를 프롬프트에 동봉해 합성 단계가 반영하게 한다. 플레이북 채택은 R5가 보수적으로(빈 배열=정답).
- **해소책:** 논제 레벨 R4(페르소나 성적표 입력 포함) 구현 또는 운영자 결정으로 현 구조 승인.

---

## 결합/구조

### 🟢 STRUCT-1 — 트레이딩 로직의 언어/결합 (결정됨)
- **결정(2026-06-08):** **별도 Python 두뇌 + openclaw=exec 스케줄러.** 트레이딩 로직은 설계서대로 Python(pydantic v2 데이터 계약·mypy strict·PostgreSQL append-only). openclaw cron/heartbeat가 슬롯마다 `python -m trading.run <round>`(연속 감시는 `trading.watch`) exec. 라운드 간 전달은 DB/파일(설계서 §3). 알림은 openclaw 채널(Telegram).
- 제외: 설계서/M2의 `src/scheduler` 자체 스케줄러(SCHED-1로 폐기). 귀결: SCHED-3(모델 라우팅)도 Python 직접 오케스트레이션으로 결정.

### 🟢 EXEC-1 — 자동 집행 전환: 운영자는 보고만, 시스템이 주문까지 (운영자 결정 2026-07-13)
- **결정:** §6 "수동 결재·수동 집행(의도된 마찰)"을 **자동 승인+거부권 / 자동 집행**으로 전환.
  운영자 확정 파라미터: ① 승인=자동+거부권(`--veto`, 다음 거래일 09:00 전) ② 하드캡=**종목당 100만원·총 500만원·동시 5종목·일일 신규 2건**(코드 강제, env는 하향만) ③ **dry-run 5거래일 검증 후 운영자가 EXEC_MODE=live 전환** ④ 브로커=토스증권 Open API(키 발급됨).
- **구현(2026-07-13):**
  - `collectors/toss.py` — OAuth2+토큰캐시·계좌·시세·**지정가 주문·조건부 청산(SINGLE·SELL)**. 시장가 경로 부재(orderType=LIMIT 하드코딩, 회귀 테스트로 가드) — 절대금지 #3 유지.
  - `executor.py` — 캡 검사→지정가 매수(즉시 트랜치 비중만, 확인 트랜치 v1 미집행)→체결 폴링(`reconcile`)→**조건부 손절 자동 등록(트리거 2틱 아래 지정가)**→PositionRecord 자동 박제. append-only 저널 `data/exec.sqlite`. 킬 스위치 `.runtime/exec/KILL`.
  - `watch/arm_watch.py` — 발동 감지 → `execution_pass`(집행+추적)를 감시 패스에 연결(집행 오류는 감시를 죽이지 않음). 가격은 토스(주문 브로커와 동일 소스)→KIS 폴백, 결측 시 집행 스킵(추측 금지).
  - `run.py` synth 직후 자동 승인 + P0 통지(거부 안내·기한 동봉). `approve.py --veto`(approved→**vetoed**, 계약 상태 추가) — 활성 풀 제외.
- **2차 개정(운영자, 2026-07-13 같은 날):** **투자 정책 하드캡 전면 제거** — "계좌에 500만
  고정 예치, 분석이 통과시켰다면 계단식·분산 무관". ① 종목당·총액·동시보유 캡 삭제(계좌
  잔고=자연 상한, live는 `buying_power_krw` 실조회) ② 사이징 = 가용액 × **R5 초안 cap 계수**
  ("0.5 * normal_unit"의 0.5, normal_unit=계좌 기준액 `EXEC_ACCOUNT_KRW` 기본 500만) × 즉시
  트랜치 비중 — **분석이 사이즈를 정한다** ③ **최소 1주 보장**(고가 종목이 계수 때문에 못
  사지는 상황 방지 — 운영자) ④ 같은 종목 재진입(새 초안) 허용=계단식 ⑤ 폭주 가드만 잔존:
  일일 신규 5건·초안당 1회 dedup·킬 스위치(투자 정책 아님 — 오작동 방어).
- **미국 주식(운영자 결정):** KR live 검증 후 별도 마일스톤로 — 워치리스트(ETF·메가캡)부터.
  토스 API는 US 지원(프리·정규·애프터, LOC) 확인됨. 지금은 KR 전용.
- **P-11 Stage B(2026-07-13 밤):** 익절 자동화 — 체결 시 손절 단독 SINGLE → **OCO(손절+익절, R:R=EXEC_RR 기본 1.5)**. R5 초안 풀 5→8 + 대안 셋업 권장. `sector_ignition` 흐름변수(토스 실시간 랭킹×KRX 업종) 신설 — R5가 오늘 밤부터 조건에 사용 가능.
- **안전 잔여(🟡):** 손절이 지정가라 갭다운 미체결 위험(지정가-only 제약의 본질 — 트리거 2틱 아래로 완화). 확인(condition) 트랜치 미집행. 임의 청산(스탑·익절·시간손절 외) 자동화 없음. KIS 경로 병행 여부는 토스 실운용 후 판단. dry-run 잔고는 보수 근사(청산 미반영 — live는 실 매수가능금액).
- ~~**dry-run 시작: 2026-07-14(화) 예정** — 5거래일(7/21 월요일 리뷰) 후 live 전환 판단.~~
- **2026-07-14 운영자 결정: dry-run 조기 종료 — `EXEC_MODE=live` 전환(10:05 KST, "dry run 다 빼").**
  - **배경(사고 2건, D1 오전):** ① 7/13 21:48 운영자 지시("내일 … 소액으로라도 진입하도록")가
    `EXEC_TEST_ENTRY` **dry-run 전용** 계측으로 격하 인코딩됨 — 기존 "dry-run 5거래일" 결정과의 충돌을
    되묻지 않고 임의 해석(규칙 위반). 09:54 뉴파워프라즈마 실발동(9,960)이 dry-run 기록으로만 남았고
    한 시간 내 +4.7% 구간을 놓침(운영자 손실 항의). ② 게이트웨이 tmux env 스냅샷(7/13 02:05)이
    이후의 .env 갱신(토스 키·EXEC_*)보다 낡아 cron 감시기가 토스 미설정·레짐 UNKNOWN으로 기동(09:00~09:54).
  - **조치:** 감시기·게이트웨이 재기동(live env 상속 확인), `EXEC_TEST_ENTRY` 제거(.env).
    검증창을 잃었으므로 **live 첫 주(7/14~7/21)는 사실상 검증 겸 운영** — 첫 실주문·브래킷·사다리 경로를 기록으로 밀착 확인.
  - **재발 방지(세션 규칙):** ① 운영자 지시를 좁혀/보수화해 인코딩할 때는 **실행 결과를 지시자 언어로
    에코백 확인 필수**("내일 실제 주문은 나가지 않습니다" 수준의 결과 문장). ② **.env 변경 시 게이트웨이
    재기동이 운영 절차** — env는 기동 시점 스냅샷이다(INFRA 후속: sync가 .env mtime vs 게이트웨이 기동시각 경고).

### 🟢 EXEC-2 — 계단식 청산: R5 지정 레벨 + 경고 축소 + 본전 상향 (운영자 결정 2026-07-13 밤)
- **결정(운영자 제안→논의 확정):** ① 익절·손절 레벨은 고정 R:R이 아니라 **저녁 R5가 분석으로 지정**(targets 사다리 + soft_stop) ② 청산은 계단식 — 단 하방은 "밑으로 분할"이 아니라 **경고 레벨 축소(soft_stop, 부분) + 하드 스탑 전량**(무효화 규율 유지 — 분석 결과 운영자 수용) ③ **첫 익절 체결 시 잔량 손절을 본전(체결가, 틱 절사)으로 자동 상향**(결정론).
- **구현(2026-07-13 밤, R5 21:05 전 배포):**
  - 계약: `OrderDraft.targets`(ExitLevel 사다리 — 순증가·합≤100)·`soft_stop`(하드 위·<100%) — 선택 필드(구 레코드 호환).
  - R5: 프롬프트에 targets/soft_stop 스키마+grounding 규칙(근거 가격 컨텍스트의 저항·전고만, 불확실=생략) — **오늘 밤 산출부터 포함**.
  - 집행: 브로커에 **전량 보호 브래킷 상주**(하드스탑+최종타깃 OCO — 워처 사망에도 보호) + 감시 루프 `manage_exits`가 부분 레그(중간 타깃·경고 축소)를 지정가 매도 → **브래킷 취소·재등록**(잔량·본전 상향). 레그당 1회 dedup·패스당 1레그·잔량<2주면 사다리 강등.
  - 토스: `cancel_conditional`(DELETE) 추가. R:R(EXEC_RR)은 R5가 targets 미지정 시 폴백으로 강등.
- **잔여 해소 + EXEC-3(같은 날 밤 2, 운영자 결정 — 애프터 참여 범위):**
  - **결정: 애프터마켓(NXT ~20:00)은 청산만, 진입 금지.** 근거: arm 조건(체결강도·호가)이 정규장 데이터 기반이라 애프터에선 평가 불가 + 유동성 얇음. 청산은 가격 조건뿐이라 유효 — 악재의 애프터 손절이 익일 갭다운보다 우위.
  - 감시 창 이원화: **진입 09:00~15:00 / 청산 전용 09:00~20:00**(15:00~15:30 공백도 해소). cron `arm-watch-exit`(15:35 재기동 안전망) 추가 — 19잡.
  - **부분 레그 체결 확인(v1.1) 구현**: 레그 주문 orderId 추적 → 패스마다 폴링 → FILLED=leg_fill 박제 / PENDING인데 호가 이탈 시 취소→현재가 재호가 / CANCELED·REJECTED는 P1 보고.
  - 잔여(🟡): 토스 조건주문(브래킷)이 애프터 세션에서 트리거되는지 스펙 미명시 — 청산 전용 감시가 헤지하지만 실측 확인 항목. 20:00 이후~익일 09:00은 브래킷만 상주.

### 🟢 EXEC-4 — 주당 가격 상한 + 갈아타기(rotation) (운영자 결정 2026-07-13 밤 3)
- **결정:** ① **주당 200만원 초과 종목 배제**(계단 청산이 물리적으로 불가한 1주 포지션 방지 — `EXEC_MAX_PRICE_KRW`, 경계 포함=허용) ② **갈아타기**: 신규 발동이 잔고 부족일 때 기존 포지션 중 열등한 것을 팔아 자금 확보 후 진입.
- **가드레일(구현·전부 결정론):** 러너 보호(첫 익절 체결=본전 상향 포지션 교체 불가) · 매도 우선순위=손절 2% 이내 근접은 잔여가치 0, 그 외 잔여 상승여력 최저 · **교체 마진**: 신규 계획 여력 ≥ max(잔여 여력×2, +2%p) — 왕복 비용·churning 차단 · **일 1회**.
- **동반 수정:** 발동 시 잔고 부족 초안이 영영 미집행되던 버그 → 매 패스 재시도(`cash_skips_today`). dry-run 잔고 근사에 교체 매도 대금 반영(`committed_krw` 차감).
- **잔여(🟡):** live에서 교체 매도 대금의 매수가능금액 반영 시차(체결 전 재시도는 잔고 부족 재스킵 → 다음 패스 자연 해소). 교체 매도 자체의 체결 확인은 레그 v1.1 경로와 별개 — dry-run 관찰 후 필요 시 통합.

### 🟢 EXEC-5/6 — 배분: 동적 풀 비례 + 회수 사다리 (운영자 설계 2026-07-13 밤 4~5, 2차 개정)
- **경위:** ①발동 순서가 배분을 정하는 문제 지적 → 고정 분모 풀 비례(순서 불변)로 1차 개정 → ②운영자 재설계: "자본 가동률 우선 — 앞은 과감히, 뒤 트리거는 앞 포지션 일부 회수로 자금 조달, 남은 트리거 수에 따라 몫 동적 조절".
- **확정 구현:**
  - **동적 몫**: 배분 = 가용 잔고 × (자기 계수 ÷ **미집행 잔여** 활성 풀 계수 합) — 잔여 트리거가 줄면 몫 자동 증가. 순서 영향은 ±소폭 잔존(공정성보다 가동률 우선 — 운영자 선택).
  - **회수 사다리(잔고 부족 시)**: ①갈아타기(EXEC-4, 전량 교체 — 열등·손절근접) → ②**부분 트림(EXEC-6)**: 잔여 상승여력 최저 포지션에서 **부족분만** 매도(러너 제외·손절근접 제외·포지션당 50% 상한·브래킷 잔량 교체) → ③그래도 부족하면 다음 패스 재시도.
- **진화 경로:** R5 계수 차등(확신 0.3~1.0) 도입 시 확신 가중으로 자연 전환. 유휴 장기화 시 P-12(코어 ETF) 별도.

### 🟢 EXEC-7 — 시장 레짐 채널 신설 (7/13 -8.9% 폭락 사후 — 운영자 승인 "모두 진행")
- **근본 원인(조사 확정):** R4 기각은 오판이 아니었다 — "마감시황=이미 가격 반영된 사후 보도, 촉매 부적격"은 임무 내 정당 판정. **결함은 레짐 정보가 흐를 채널이 촉매 파이프라인뿐**이어서, 기각 순간 시스템 전체(R5·집행)가 폭락을 모르게 된 구조 + R5 입력이 T-1 EOD뿐인 신선도 문제.
- **구현(2026-07-13 밤):**
  - **A. 레짐 채널**: `regime.py`(순수 코드) — 토스 지표 실시간 vs 전일 종가 → 코스피 당일 ≤-3% CAUTION(신규 배분 절반)/≤-5% RISK_OFF(신규 중단)/관측불가 UNKNOWN(=CAUTION 취급). 감시 패스마다 판정, 비정상 레짐 하루 1회 P0. 청산 관리는 레짐 무관 계속. 임계 env(REGIME_*).
  - **B. 입력 신선도**: `live_backdrop_lines()` — R5 합성·R6 보고 직전에 당일 실시간 지수+레짐 경고를 백드롭에 병기(결정론). 라이브 검증: 폭락 당일 RISK_OFF·-8.9% 정확 판정.
  - **C. 기각 오독 방지**: 저녁 보고 기각 라벨에 "촉매 부적격 포함 — 사실 판정 아님" 명시.
  - **+D1 계측 장치**(운영자 요청): `EXEC_TEST_ENTRY=1`(dry-run 전용·임시) — 11시까지 자연 발동 0이면 가드 통과 초안 중 계획 R:R 최고 1건을 **1주**로 관통시켜 체결→브래킷→사다리 전 경로 계측. 검증 후 제거 예정.
- **잔여(🟡):** 서킷브레이커 이벤트 자체의 감지(지수 임계와 별도 신호)·환율 임계는 M4 감시기 패스로 후속. R4 기각 사유의 구조화 필드(사실반박/촉매부적격 구분 코드화)는 R4 스키마 확장 시.

### 🟢 EXEC-8 — 재진입 정책 + 진입 가격 밴드를 계획(R5)이 명시 (운영자 제안·결정 2026-07-14) — 코어 구현 완료(당일)
- **운영자 제안:** ① 저녁 계획이 중복(재)진입 허용 여부를 명시 — 불허=1회 진입 후 종료,
  1회 허용=청산 후 타이밍 재도래 시 1회 한정 재진입. 단 **동일 종목 기보유 시 재진입 금지**.
  ② 계획이 익절·손절 레벨로부터 **매수 유효 가격 범위**를 사전 산출 — 트리거가 통과해도
  현재가가 익절/손절에 ±1% 수준으로 붙어 있으면(수수료 포함 시 익절이어도 손해) 진입 스킵.
- **검토 보완(세션 분석):** 기존 R:R 가드는 손절 초근접 진입을 오히려 통과시킴(위험이 작아
  R:R이 커 보임 — 905원 진입/손절 900 = R:R 59) → 밴드가 양방향을 완성. 기보유 판정은
  내부 장부가 아니라 **브로커 실측(`holdings()`)** — A6(브래킷 체결 동기화) 선행 필수.
  재발동은 엣지 트리거(조건 꺼짐→켜짐) + 청산 후 쿨다운(기본 30분).
- **운영자 확정(3갈림길, 2026-07-14):**
  - **하드 스탑 청산 후 재진입 금지 고정** — 손절=무효화 라인 붕괴, 같은 초안 재진입은 §6 규율 위반.
    재진입은 익절·본전 상향 청산 후에만.
  - **밴드는 코드 결정론 산출 + R5는 조이기만**(넓히기=계약 위반 폐기). 산식: 하한=손절×(1+최소이격 1%),
    상한=가중 보상(사다리 pct 가중) 기준 잔여 R:R≥1 + 수수료·거래세 마진. 감사 C4(최종 타깃 과대평가)도 함께 해소.
  - **재진입 사이즈 체감 50%** — 같은 셋업 2회차는 신뢰도 하향(보수).
- **계약 변경(안):** OrderDraft += `max_entries: 1|2`(기본 1), `entry_band: {low, high} | None`(코드 산출,
  R5는 범위 내 조임만). 붕괴·소진·R:R 가드 3종은 밴드 검사 1개로 통합(구 초안은 기존 가드 폴백).
  저녁 결재 다이제스트에 재진입 정책·유효 범위 표기.
- **선행:** 가드 감사 A묶음(A1 브래킷 원자성·A2 부분 체결·A6 체결 동기화·A7 미체결 매수)·B묶음(mode/day
  오염)·전일고가 캐시 — 2026-07-14 승인·구현 완료.
- **구현(2026-07-14, 커밋 4번째):** OrderDraft `max_entries`(1|2)·`entry_band`(R5 조임만 — 확장은 폐기),
  `derive_entry_band`(하한=max(하드,경고)×1.01 / 상한=min(가중 보상 R:R≥1, 익절1×0.99)) — 붕괴·소진·R:R
  가드 3종을 밴드 1개로 통합(C4·C5 동시 해소). 재진입: rearm 채널(arm_watch)→execute_armed 가드(기보유·
  청산 미확정·하드스탑·쿨다운 30분, 전부 비저널)→체감 50%. 손절 지정가 플로어 max(2틱, 1%)
  (`stop_order_price`, EXEC_STOP_FLOOR_PCT). 결재 다이제스트에 매수범위·재진입 표기.
- **잔여(🟡, 실측 게이트):** ① **분할 OCO**(OCO#1 경고/익절1 + OCO#2 하드/최종 — 익절1도 브로커 상주,
  운영자 승인) — 토스가 동일 종목 복수 조건주문(수량 분할)을 허용하는지 **live 첫 체결 때 실측** 후 구현.
  불허면 현행(전량 OCO + 감시기 익절1) 유지 — 단순 상주 지정가는 손절 취소 경주 때문에 금지.
  ② 미체결 스탑 백업(감시기 재호가 추격) — 조건주문 상태 스키마 실측 후.
  ③ 재진입 엣지 트리거(조건 꺼짐→켜짐 요건)는 쿨다운 30분으로 단순화 — 실운용 관찰 후 필요 시 추가.
  ④ dry-run 한정: committed_krw가 청산 미반영(보수 근사)이라 dry-run 재진입은 잔고 부족으로 막힐 수
  있음 — live는 실 매수가능금액이라 무관.

### 🟢 EXEC-9 — 시간손절 자동 집행: 도래일 14:30~14:50 창 (운영자 결정 2026-07-14)
- **배경:** 시간손절이 [정리 검토] 플래그만 띄우고 매도는 수동이었다(가드 감사 D2 —
  도래일에 브래킷도 만료돼 방치 시 무방비). EXEC-1 "운영자는 보고만" 철학과 불일치.
- **결정:** 도래일(놓치면 그 이후 첫 거래일) **14:30~14:50 창**에서 자동 전량 매도.
  15:00 직전은 단타 등 변수가 많아 회피(운영자). 시간은 거래일 기준(add_trading_days —
  주말+KRX 휴장 스킵).
- **구현(당일):** `executor.manage_time_stops` — 브래킷 취소→지정가 매도(A1: 취소 실패=중단·
  보호 유지, 매도 실패=손절 단독 원복), 미체결 추격은 레그 재호가 루프(kind `leg_timestop`),
  포지션 close_reason="시간손절 자동 집행" 박제 + P0. 브래킷 만료는 시간손절 **+1거래일**로
  등록(만료 시각 스펙 미확정 — 집행 창을 확실히 덮는다). bracket_gone 초안은 자동 매도 금지(보수).
- 참고: close_reason에 "손절"이 포함돼 EXEC-8 재진입도 자동 차단된다(미진행 셋업 재진입 방지 — 의도).

### 🟢 EXEC-10 — 아침 진입 규율 + 모멘텀 가드 + 최소 배분 하한 (운영자 결정 2026-07-15)
- **배경(7/15 아침 실측 3건):** ① 09:01:48에 S-Oil·피에스케이 진입 — 개장 직후 변동성·지표 왜곡
  구간 ② 각 1주씩만 매수(가용 150만+) — 동적 풀 비례가 소액 계좌+풀 8건에서 배분을 ~22만으로
  쪼갬(최소 1주 보장이 하한 역할) ③ 운영자: 급등 추격·급락 칼 진입 위험.
- **결정(운영자):**
  - **진입 평가 09:10부터**(`WatchConfig.entry_start`) — 그 전엔 청산·체결 추적만. 09:00~10:00
    폴링 90→**60초**(`fast_poll_seconds`).
  - **급등 차단**: 당일 등락률 ≥ +5%(EXEC_SURGE_MAX_PCT)면 진입 보류.
  - **급락 회복 공식**: 당일 저점 등락률 ≤ -5%면 급락 — **저점 낙폭의 40% 회복 후에만 진입**
    (기준 = 저점 등락률 × 0.6; -5%→-3%부터, -10%→-6%부터). 저점·전일 종가는 KIS 일자별 실측.
  - **모멘텀 보류는 재시도 skip**(`retryable_skips_today`) — 회복 확인되는 패스에 진입
    (armed 1회 소비와 무관). 관측 불가면 보수 보류.
  - **최소 배분 하한**: 진입당 min(25%, max(15%, 50%/n)) × 가용액, **n = 감시(미집행 활성)
    + 보유 종목 수**(운영자 교정 — 감시만 세면 기보유 무시 과집중). 2종목 이하 25%
    (잔고 50% 사용 보장), 4종목+ 15%. '1주씩 사는' 문제 해소.
- 참고: 같은 날 실사고로 어댑터 결함 추가 교정 — 조건주문 취소(DELETE) 응답이 빈 본문인데
  json 파싱 크래시 → 호출측이 '취소 실패(보호 잔존)'로 오판(실제론 취소됨 = A1 역전).
  빈 본문은 None 반환으로 수정(PSK 수동 4주 편입 중 적발).

### 🟢 EXEC-11 — 저잔고 모드 + 교체 문턱 개정(가중 수익률 Δ) (운영자 결정 2026-07-15)
- **저잔고 모드**: 가용액 < 계좌 기준액×30% → 진입 배분 = **계좌 기준액×15%**(하한 공식 대체,
  가용액 초과 불가·최소 1주 보장 유지) — 마지막 현금을 한 종목에 몰지 않고 ~2회 여력 보존.
- **교체(갈아타기) 개정**: 기존 `신규 ≥ max(보유잔여×2, +2%p)`(과보수 — 사실상 교체 불발) →
  **Δ = 신규 − 보유잔여 ≥ 1.5%p**(운영자 1%p + 왕복 실비 0.5%p 가산, EXEC_ROTATE_MARGIN).
  양쪽 수익률은 **사다리 가중**(`weighted_remaining_upside` — 최종 타깃 단독 부풀림 제거, C4 계열).
- **Δ 미달 = 트리거 당일 폐기**: `rotation_reject` 박제 — 그날은 재시도·부분 트림 모두 생략
  (왕복 소음 방지), 다음 날 조건 재충족 시 부활(veto 아님). 러너 보호·손절 근접 가치0·일1회는 유지.

### 🟢 SECT-1 — 종목 섹터 기준을 KRX 공식 업종으로 전환 (운영자 결정 2026-07-13) — 🟡 잔여 있음
- **결정:** 종목 섹터 태그의 최우선 기준을 **KRX 공식 업종분류**(KIS 현재가 시세 `bstp_kor_isnm`, 실호출 관측)로 전환 — "자체 태깅보다 금융사/거래소 분류 신뢰"(운영자). 토스 공식 Open API는 업종 필드 자체가 없어 기준 후보에서 제외(스펙 전수 검색 0회).
- **구현(2026-07-13):** `sectors.classify_krx` → `kis-bstp-v1` 소스 적재(거래소 원문 그대로, 정규화·추측 없음), `SECTOR_SOURCES` 최우선 배치(first-wins — 기존 큐레이션·KSIC·LLM 폴백은 KIS 결측 종목 갭 필로 강등). daily-eod 체인(`sectors.main`)이 매일 신규분 태깅. 첫 실행: 게이트 232종목 중 231 태깅(21개 업종).
- **🟡 잔여(계약 §4 층 — 별도 결정 필요):** `FactRecord.sector`(Sector enum)·R2 뉴스 이벤트 `sector:` 라벨은 **29버킷 taxonomy 유지**. 스윙 열기·factpack·discuss·스크리너는 문자열 그룹 키라 전환 완료, 이벤트↔종목 조인은 종목코드 기반이라 무영향. 계약까지 KRX 업종으로 바꿀지(=설계서 §4 변경), taxonomy를 뉴스 분류 전용으로 남길지는 운영하며 결정.
- **트레이드오프(고지됨):** KRX 업종은 테마 해상도가 거칠다 — 반도체 장비주가 기계·장비/의료·정밀기기로 분산, 지주사가 금융으로 흡수(LS 등). P-9 도메인 점화 축은 KRX 버킷 기준으로 동작.

---

## 인프라 / 이식성

### 🟢 INFRA-1 — 트레이딩 전용 openclaw 인스턴스 격리 (결정됨)
- **결정(2026-06-08):** 트레이딩은 개인 `~/.openclaw`(개인 auth·cron·스킬)와 **분리된 전용 openclaw 인스턴스**를 쓴다. 프로젝트 `OPENCLAW_HOME`(예: repo의 `.runtime/openclaw`, gitignored)에 상태/자격증명을 두고 openclaw 바이너리는 공유. 개인 환경과 안 섞여 git 100% 관리·이식 가능.

### 🟢 INFRA-2 — GitOps 완전 이식성 (결정됨)
- **결정(2026-06-08, 운영자 요구):** repo가 시스템의 **단일 소스**. 다른 기기에 `git clone → ops/bootstrap.sh → 1Password로 .env → 즉시 가동`.
- openclaw 설정(cron/heartbeat/채널/openclaw.json 템플릿)을 런타임에 손으로 두지 말고 **`ops/openclaw/`에 선언적 코드**로 두고 idempotent sync 스크립트로 적용.
- `ops/bootstrap.sh`: openclaw 설치(핀 Node 22.22) + poetry install + openclaw config 적용 + cron 등록 + `.env`(1Password/op).
- **git 제외는 단 둘**: 비밀값(1Password), 생성 런타임 상태(`.runtime/`). 버전 핀(Node 22.22, Python 3.13, openclaw, `poetry.lock`) 필수.

---

## 외부 의존 (Phase 1, 미해결 — 부록 A)
- 🟢 ~~KRX 정보데이터시스템: 시세·투자자별 매매동향~~ → **해소(2026-06-11)**: 시세=data.go.kr(기 운영), 수급=KIS TR(COLLECT-2 갭① 해소 참조). KRX 직접 접근 불필요. (공식 KRX Open API엔 투자자별 매매동향 자체가 없음 — 29개 서비스 목록 확인)
- 🔴 NXT 프리·애프터마켓 데이터: 소스·접근
  - **2026-07-13 조사(토스증권 Open API):** 장 캘린더 `GET /api/v1/market-calendar/KR`이 **KRX+NXT 통합 세션**(프리마켓 08:00~·부분휴장 포함)을 공식 제공. 1분봉 캔들(OHLCV)이 NXT 프리마켓 체결을 포함하는지는 **키 수령 후 실측 필요** — 포함이면 `premkt_volume_ratio` 산출 경로 확보(SEL-1 부분 해소 후보). 스펙: `openapi.tossinvest.com/openapi-docs/latest/openapi.json`.
  - **2026-07-13 저녁 실호출 관측:** `/prices`가 **NXT 애프터마켓 체결을 반영**(005930 timestamp `19:59:59+09:00` — 정규장 마감 후 시각). 통합 세션 시세임이 실증 — **프리마켓(08:00~08:50) 반영 여부만 다음 거래일 아침 실측하면 SEL-1 판정 완료.**
- 🔴 증권사 조건부(청산) 주문 API: 보유 증권사 스펙 확인 선행 (Phase 1 필수 — §6)
  - **2026-07-13 조사(토스증권 Open API — 운영자 신청 완료):** 공식 조건주문 API 확인 — `POST /api/v1/conditional-orders`, 타입 `SINGLE/OCO/OTO`, `triggerPrice` 감시→`LIMIT`(지정가, `orderPrice` 필수)/`MARKET` 발주, `expireDate` 만료, 멱등키 지원. **OTO(진입→체결 시 손절 자동 등록)·OCO(손절+익절 동시)가 §6 청산 요구와 정합.** 어댑터에서 `orderType=LIMIT`만 허용하면 절대금지 #3(시장가 금지) 계약 강제 가능. 제약: 종목당 OCO/OTO 1개·계좌계열 초당 1회·IP 등록 필수·REST only(웹소켓 없음). **채택 여부는 운영자 결정 대기**(KIS 경로와 택일 또는 병행).
- 🟢 DART OpenAPI / 한국은행 ECOS: 공개 문서 기반 구현 가능

---

## 전략 피벗 (2026-08-26 — v0.3)

### 🟢 PIVOT-1 — 전략 전면 전환: 단타·스윙 → 장기 사이클·가치 투자 (운영자 결정 2026-08-26)
- **결정(운영자):** 단타/스윙으로는 개인이 기관·외국인을 이길 수 없다 — 개인의 구조적 우위(환매
  압력 없는 시간 지평)를 쓰는 **장기(N년) 사이클·가치 투자**로 전면 전환. 로우 사이클 산업에서
  향후 하이 사이클 후보를 발굴한다. 상세 확정안은 PROPOSALS **P-14 확정·개정 블록**.
- **거버넌스 확정:** 종목 편입·제외·비중·무효화는 **검증 가능한 기계적 규칙(순수 코드)**이 결정.
  사람도 LLM도 종목을 심사하지 않는다. 운영자 권한 = **규칙(정책) 승인 · 자금 한도 · 거부권**.
  결측 지표의 수동 입력(예: PER 수기 백필)은 데이터 입력 경로로 허용(판단 아님).
- **비준·마이그레이션 완료(2026-08-26, 운영자 "v0.3 비준한다"):** ① 설계서 v0.3이
  `trading-system-design.md`로 교체됨(v0.2·초안은 git 이력) ② CLAUDE.md 개정(확정 아키텍처 —
  판단 전부 순수 코드, LLM=수집+서술만, 디렉터리 구조 v0.3화) ③ `.claude/session.md` 개정
  (스윙 운영 절차 폐기) ④ 스윙 스킬 5종(approve·arm-check·work-boot·positions·discuss) →
  `.claude/skills-frozen/` 이동 ⑤ `ops/openclaw/FROZEN.md` 동결 고지(cron 등록 금지).
- **경과 조치:** v0.2 스윙 파이프라인(장중 감시기·브래킷·EXEC-8~11 규율·cron 16슬롯)은 **재가동
  금지(동결)** — 코드는 수정·삭제 없이 보존(`watch/`·`selector/`·executor 스윙 경로). 현재 환경
  미프로비저닝(8/26 확인: .env·.runtime·poetry·openclaw 부재). 스윙 계열 🟡 잔여는 폐기(파일
  상단 고지). 다음 실행 단계 = Phase 0(환경 복구·PIVOT-6 브로커 대사).

### 🔴 PIVOT-2 — 사이클 실물 지표 데이터 소스 미확정 (산업 온도계 입력)
- **필요:** 산업별 사이클 위치 판정용 실물 지표 — 수출입 통계(관세청/무역협회), 재고·출하·가동률
  (통계청 광공업동향), 가격 스프레드(DRAM 고정가·정제마진·운임지수 BDI/SCFI 등), CAPEX 동향.
- **잠정 처리(절대금지 #1):** 어댑터 인터페이스+스텁으로만 진입, 엔드포인트 추측 금지.
- **2026-08-26 강등(PIVOT-7 ②):** 실물 지표는 온도계의 **게이트가 아니라 보강 축(가점·조기
  신호)** — 편입 판정 기준은 자체 히스토리 밴드 3축(섹터 PBR·마진·매출, 백필 데이터)이므로
  이 항목이 미해소여도 시스템은 가동된다. 단 밴드는 본질적으로 후행하므로 조기 신호 축으로서
  가치는 유지 — 산업별 지표 1개씩(조선=수주잔고/신조선가, 해운=운임지수, 반도체=DRAM 고정가
  등) 소스 확정을 후속 작업으로 지속.
- **2026-08-26 수동 입력 우회(PIVOT-8):** 어댑터 확정 전에도 **운영자 수동 입력 채널**
  (`source="manual:<출처>"`, 설계서 §4 가드)로 보강 축 가동 가능 — 월 1회 지표값 입력.
  자동 어댑터 확정 시 manual보다 우선으로 교체.

### 🟡 PIVOT-3 — DART 재무·주주환원 확장 API 확정
- **필요:** ① 자기주식 취득·처분·**소각**(주요사항보고서 의무 공시 — 존재는 확실, API 확정 필요)
  ② 배당(정기보고서 배당에 관한 사항) ③ ~~PBR/PER/PSR 산출용 상장주식수·시총 결합 경로~~
  → **해소(2026-08-26 실측):** data.go.kr 일별시세 기 적재 필드 `mrktTotAmt`(시총)·
  `lstgStCnt`(상장주식수)로 완결(market.py `_SRC` 관측) — 신규 소스 불필요, `valuation/build.py` 사용 중
  ④ ~~재무 히스토리 백필 범위~~ → **실측 확정(2026-08-27):** 연간 재무 10년 백필 완료 —
  222종목·1,834건 적재, **10년 완비 145/222·중앙값 10년**(미만분은 상장 이력 짧은 종목 —
  결측 정직). 시세(시총) 스냅샷은 **data.go.kr 커버리지가 2020년부터**(2016~2019 무자료 실측)
  → 섹터 PBR 밴드 창 = 2020~현재(7개 연도점), 마진·매출 밴드 = 10년. 연말 스냅샷
  2020~2025 + 최신일 적재 완료.
- **잠정 처리:** 기존 `dart.py`·`fins.py`(fnlttSinglAcnt) 확장으로 접근, 정확한 API명·파라미터는
  DART 공식 문서에서 확정 후 구현. 취득≠소각 구분 필수(소각·배당 가중, 미소각 보유=오버행 감점).
  밸류업(기업가치 제고 계획) 공시는 거래소 KIND 채널 가능성 — 채널 자체도 확인 대상.

### 🟢 PIVOT-4 — 수급 장기 코호트 세분 (연기금 구분) — **2026-08-27 기해소 확인**
- **맥락:** P-14 확정안 ④ — 수급은 네거티브 스크린+타이밍 보조로 강등. 장기 주체(연기금)와
  단기 주체(금융투자 프랍)가 "기관"에 합산되면 신호가 희석된다는 우려였다.
- **확인(코드 실측):** 이미 해소돼 있었다 — flows 파이프라인이 KIS TR 응답의 **기금(연기금)
  세분을 `raw_json`으로 전량 보존**하고 `fund_ntby_tr_pbmn`(KIS 공식 라벨 "기금 순매수 거래
  대금")을 추출해 소비 중(`flows.py` — 기관계=금융투자+투신+사모+은행+보험+종금+기금 실관측
  합산 검증 주석, 테스트 `fund_ntby_mn` 커버). raw_json에 나머지 세분(금융투자·투신 등)도
  보존돼 있어 필요 시 추출만 추가하면 된다.
- **v0.3 적용:** 네거티브 스크린은 3분류(외인+기관계) 60~120거래일 누적, **연기금 코호트는
  `fund` 세분으로 타이밍 보조 가점** — 신규 소스 불필요.

### 🟢 PIVOT-5 — 사이클 화이트리스트 큐레이션 — **2026-08-27 결재(policy-v1.0)**
- **결재:** ① 조선=(b) 종목 큐레이션(HD현대중공업·삼성중공업·한화오션 — 버킷 혼합 실측 후
  명시 그룹) ② 화이트리스트↔KRX 버킷 매핑표 승인(8개 산업, 1단계 제외 목록 포함)
  ③ 실물 보강 축 metric 키 + 월 1회 수동 입력 승인 ④ 검증 사이클=운송·창고 2024 →
  **검증 PASS**(as-of bottoming, 임계 사전 고정·조정 이력 없음). 원본: `docs/POLICY_PARAMS.md`
  v1.0, 코드 동기: `src/trading/cycle/policy.py`. 미결(§5 — R4 임계 등)은 Phase 3 결재.

### 🟡 (구)PIVOT-5 경위 — 큐레이션 (기록)
- **기준(2026-08-26 개정 — PIVOT-7 ②와 정합):** 편입 기준 = **산업 특성 큐레이션**(설비투자
  리드타임·가격 수용자(commodity)·글로벌 수요 연동). 1차 판정 축(자체 히스토리 밴드)은 전
  섹터에 존재하므로 "실물 지표 존재"는 편입 요건에서 제외 — 실물 지표는 산업별 **보강 축
  매핑표**로만. 비사이클(바이오·게임·엔터·플랫폼)과 방어(음식료·통신·유틸)는 1단계 제외.
  구조적 사양 판정은 섹터 합산 매출 장기 추세(자체 데이터) + 운영자 제외 목록.
- **후보(제안 — 승인 전 가동 금지):** 조선·해운·화학·정유·철강·메모리반도체·건설기계·은행/증권
  (금리 사이클). 산업 큐레이션 + 보강 축 매핑표를 운영자 결재에 올린다(Phase 2 산출물).
- **2026-08-27 결재 패킷 산출:** `docs/POLICY_PARAMS.md` 초안 v1 — R3 제안 파라미터,
  화이트리스트↔KRX 버킷 매핑(혼합 한계 고지: 조선↔운송장비·부품 자동차 혼합 심각 — 결정
  요청 ①), 실물 보강 축 metric 키, 리플레이 사이클 재선정안(결정 요청 ④). R3 엔진·리플레이는
  구현·실산출 완료(22산업 CycleRecord, 전기·전자 23→25 사이클 재현) — **결재만 대기**.

### 🟢 PIVOT-9 — 집행 브로커 단일화: 토스 전용, KIS는 조회 전용 (운영자 결정 2026-08-27)
- **결정:** KIS로는 실제 투자를 진행하지 않는다 — **주문·계좌 경로 영구 배제**, KIS의 역할은
  투자자매매동향(수급) 조회뿐(`KIS_APP_KEY/SECRET`만 필요). 집행 브로커는 **토스 단일**.
- **효과:** v0.2 잔여 미결 "KIS 잔고·체결 어댑터"·"KIS 경로 병행 여부(EXEC-1)" 종결.
  `.env` KIS_CANO/ACNT_PRDT_CD/ENABLE_TRADING 미소비 확정(기 표기). 코드는 이미 정합
  (kis.py 조회 전용 — 주문 TR 금지 구현·회귀 유지).

### 🟢 PIVOT-6 — 기존 포지션·브로커 대사 — **2026-08-27 실측 종결**
- **맥락:** 7/15 원장 기준 보유 피에스케이 5주·S-Oil 4주 + OCO 브래킷 상주 상태에서 시스템이
  6주 이상 무인 방치(8/26 확인). 브로커 서버 측 조건부 주문 체결·만료 여부 미상이었다.
- **실측(2026-08-27, `ops/reconcile_broker.py` 읽기 전용):** **보유 0건 · 미체결 조건주문 0건 ·
  매수가능금액 0원** — 공백 기간에 계좌 밖(수동)에서 청산·출금된 것으로 관측(시스템 기록 없음).
  정리 대상·재심사 대상 모두 소멸 → 종결. 원문 박제 `.runtime/reconcile/2026-08-27-broker.json`.
- **잔여:** 자금 재투입 시점·규모는 운영자 결정(Phase 4 집행 전 불필요). 토스 키·IP 등록은
  유효 확인(accountSeq 실조회로 .env 갱신).

### 🟢 PIVOT-7 — 정책 1차 확정 (2026-08-26 논의: 우려 7건 → 분석·합의)
경위: 세션이 v0.3 초안의 약점 7건을 제기 → 운영자 응답 4건을 **분석·판정 후**(즉시 인코딩
금지 — 운영자 지시) 보완안 4건까지 합의. 상세는 초안 각 절에 "(①~④ 보완, 2026-08-26 합의)"
표기. 확정 목록:
- **① 급락 = 긴급 재심사 트리거(손절 아님)** — 편입 평단 대비 임계(제안 -25%, 파라미터) 도달
  시 스크리너 재통과+공시 스캔. 탈락=청산 경로. 재통과여도 **원인 불명이면 DCA 잔여 트랜치
  일시정지**(매수만 중단, 다음 분기 재무 재심사 후 재개). 가격은 어떤 매도 판정에도 미입력.
- **② 사이클·저평가 기준 = 자체 과거 지표(히스토리 밴드)** — 1차 축: 섹터 PBR·영업이익률·
  매출 증감 밴드(백필 데이터, 외부 소스 무의존). 실물 지표는 보강 축으로 강등(PIVOT-2 갱신).
  bottoming = 밴드 하단 **+ 개선 모멘텀**(위치≠반전). 밴드 무결성 가드: 생존 편향(업종지수
  교차검증)·섹터 구성 급변 플래그. 구조적 사양 = 섹터 매출 장기 추세로 구분.
- **③ 자금 규모 무차별** — 계좌 규모·유동자금으로 편입·전략 분기 금지(저잔고 모드류 부활
  금지). 트랜치<1주=병합 이월, 1주>종목 상한=편입 불가(자연 제약). 소액 구간 3~5종목 수렴 수용.
- **④ 주주환원 = 경향성(track record)** — 단년 환원액 사용 금지(사이클 저점 배당 삭감 역차별
  방지). 10년 배당 지급 연수·업사이클 환원·소각 이력·정책 유지로 점수화. **분할상장(물적분할
  후 자회사 상장) 이력 = 거버넌스 감점**(기본: 10년 시효 감점, 반복 2회+=탈락 — 수위는 Phase 3
  파라미터 결재).
- **⑤ veto "침묵=진행"의 전제 = 전달 확인** — P0 발송 실패·채널 다운 감지 시 카운트다운
  미개시, 집행 보류+재시도(7/14 사고 교훈).
- **⑥ 리플레이 튜닝/검증 사이클 분리** — 임계 고정 후 검증 사이클 1회 적용, 실패 시 검증
  사이클 재사용 금지(오버피팅 방지).

### 🟢 PIVOT-8 — DB 스택 공식화(SQLite) + DB-first + 운영자 수동 입력 채널 (운영자 결정 2026-08-26)
- **결정(운영자 제안 → 분석·합의):** ① DB는 **SQLite 도메인별 파일**(`data/*.sqlite`, WAL,
  append-only 앱 계층 강제) ② **DB-first** — 라운드는 DB만 읽고 수집기는 갭만 채움(증분),
  백필은 1회 적재 영구 재사용 ③ **운영자 수동 입력을 정식 소스 채널로 격상** — API 부재
  사실 데이터(실물 지표 등)를 입력받아 축적.
- **실측 근거:** 설계서 v0.2의 "PostgreSQL"은 문서에만 존재 — journal 계층 전체·fins·flows·
  exec가 이미 `sqlite3`(psycopg 의존 자체 없음). 이 결정은 드리프트의 공식화. v0.3 부하
  (단일 운영자·직렬 배치·장중 상주 없음)에 SQLite 적합, docker/PG 제거로 Phase 0·이식성
  (INFRA-2) 단순화.
- **수동 입력 가드(설계서 §4 규약):** `source="manual:<출처명>"`·`as_of` 필수 / 단위·범위
  검증 + 전회 대비 급변 재확인(오타 방어) / 수정=새 버전 레코드 / 신선도 게이트 동일 적용 /
  자동 어댑터 확정 시 우선 교체 / **입력은 사실(지표값)만 — 점수·국면·판단 입력 경로 없음**.
- **운영 절차:** `data/`는 git 제외 유지, 백업=주기적 파일 복사. docker-compose·PG 잔재
  제거는 Phase 0 작업. 효과: PIVOT-2 보강 축이 어댑터 확정 전 수동 입력으로 가동 가능.
