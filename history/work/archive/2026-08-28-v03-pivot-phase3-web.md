# 2026-08-26 ~ 08-28 — v0.3 전면 피벗: 비준 → Phase 0~3 → 페이퍼 무인 가동 → 웹

## 배경·결정
- 운영자 선언(8/26): 단타·스윙으로는 개인이 기관·외인을 못 이긴다 — **장기(6~36개월)
  사이클·가치 투자로 전면 전환**. 우려 4건(운영자 심사 신뢰도·소각 데이터·사이클 적합 산업·
  수급 추종) 분석 후 확정: **규칙이 종목을 결정, 사람·LLM 모두 심사 배제**(운영자=정책·자금·
  거부권). 결정 원장 = OPEN_QUESTIONS PIVOT-1~10, 정책 = POLICY_PARAMS v1.2.
- 설계서 v0.3 비준(8/26) → CLAUDE.md·session.md 개정, 스윙 스킬 5종·cron 16잡 동결.

## Phase 0 — 복구·대사 (8/27)
- 환경 복구(asdf python 3.13.13·poetry·openclaw 격리 설치), PostgreSQL/docker 폐기(PIVOT-8:
  SQLite 공식화 — 코드는 이미 전부 sqlite3, 문서 드리프트 교정).
- .env 수령(맥북 tailscale 경유)·주석 오염 정리·TOSS_ACCOUNT_SEQ API 실조회 기록.
- **브로커 대사(PIVOT-6 종결): 보유 0·조건주문 0·잔고 0** — 공백기 계좌 밖 청산 관측.
- OAuth(ChatGPT 구독) 홈 디렉터리 오기록 → 레포 격리 인스턴스로 이식.

## Phase 1 — 밸류에이션 (8/27)
- 시세 246일 + 연말 스냅샷(2020~2025) → eod 체인 갭 치유가 **2021~ 전 기간 자동 백필
  (1,385거래일·380만 행)**. data.go.kr 커버리지 2020년부터(2016~2019 무자료 실측 — PIVOT-3④).
- DART 연간 10년 백필 1,834건(222종목·완비 145). ValuationRecord 222종목(PER는 연간 기준만
  — 연환산 추측 금지). 공시 원문 대조 정합(하이닉스 특이치=공시 내용).
- 계약(contracts/longterm)·R2 계산기·수동 입력 채널(급변 가드)·재무 백필 함수 구현.

## Phase 2 — 온도계·정책 (8/27)
- 섹터 히스토리 밴드(PBR 창 7y·마진/매출 10y, 짝-매칭 합산·MIN 3·무결성 가드) + 국면 엔진
  (bottoming=하단+개선, unknown 스키마 강제, secular=매출 8y CAGR≤0).
- 리플레이: **전기·전자 23:하강→24:바닥→25:과열**(메모리 사이클 선행 재현). **검증 사이클
  운송·창고 2024 = PASS**(임계 사전 고정·조정 이력 없음).
- policy-v1.0/1.1 결재: 화이트리스트 8산업(조선=큐레이션 3사 — 버킷 혼합 실측 회피),
  실물 보강 축 키, R4 임계.

## Phase 3 — 페이퍼 무인 가동 (8/27~28)
- cron v0.3 재작성(16→2잡: eod-v3 평일 18:00·weekly-v3 토 09:30), 게이트웨이·페어링·등록,
  **첫 자동 발화 ok**(8/27 18:00). 수동 드릴(`trading.drill` — status/run 델타/audit).
- R4 스크리너(탈락 전수 박제·미적용 명시) + **v1.2 가치 함정 방어**(최신 연간 ROE>0 +
  5년 중앙값 ROE≥3% — LG화학 bear 사례 계기, 통과 3→1) + 주간 다이제스트(md·HTML).
- R4.5 심사 패킷(Fable 5 서술·bear 의무 스키마 강제·환각 가드 — LG화학 가치 함정을 bear가
  정확 지적). 국면 라벨 한글화(DB는 enum 유지).
- 토스 스펙 갱신 실측(PIVOT-10): 공매도·대차·신용 공식 추가 확인 → 어댑터+일일 축적
  (첫 실행 4,820행·486콜·0실패, 당일 잠정 제외 설계). 수급 창 KIS 141종목 축적 개시.

## 웹 (P-15, 8/28 완결)
- 테일넷 전용 서버(`trading.web`, tmux) — 판정·집행 조작 없음, 서버 SVG 무의존.
- W1 대시보드(산업 온도 지도·국면 전환·R4 깔때기·신선도) / W2 종목(전 종목 테이블·상세:
  재무 10년 차트·사이클 진폭·수급/공매도/대차 추이·R4 사유·패킷) / W3 산업(밴드 차트·
  리플레이 타임라인·구성 감사)·보고서 탭·자료실(CSV 3종·원본·DB 스냅샷 스트리밍).
- 전 라우트 맥북 실검증 200.

## 검증·사고 교정
- pytest 546+ 전체·mypy strict 184파일 클린 유지. 빈 KIS_ACCOUNT_TYPE 크래시 교정(회귀
  테스트), **.env 소싱 셸 pytest 행 실사고 → conftest 실키 제거 가드**(구조 차단),
  드릴 신선도 T+1 오탐·screen 메트릭 누적 혼동 즉석 교정(반복 개선 루프 실증).

## 남은 것
- 운영자: PIVOT-10 멤버십 구성 목록(원유정제·조선사·은행·종합반도체), TELEGRAM 토큰(P0
  알림 재개 시), §6 결재(비중·DCA·거부권 창 — 페이퍼 관찰 후 제안).
- 시스템: weekly-v3 첫 자동 발화 관찰(8/29 토), 수급 60일 창 축적 → 네거티브 스크린 배선
  제안, 분기 R7, Phase 4 전환(관찰 수렴+§6+자금+운영자 명시 결정).
