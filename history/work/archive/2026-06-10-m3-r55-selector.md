# 2026-06-10 — M3: R5.5 아침 플레이북 선택기 (설계서 §3 R5.5, 순수 코드)

## 산출

### 1. 선택기 엔진 (`selector/engine.py` — 순수 함수, LLM import 0)
- `(PlaybookSet, FlowSnapshot) → SelectionResult`. 조건식 문법 `<op><숫자>`(op: <,<=,>,>=,==) — R5↔R5.5 계약.
- AND 의미론: arm_conditions 전부 충족해야 ACTIVE. **관측치 누락·평가 불가(비숫자) = 미충족**(추측 금지, 환각 가드). 기본·정상 출력 = 비거래(`no_trade`).

### 2. 러너 (`select_playbooks.py`)
- 가드: 휴장일 스킵(`require_trading_day`) + **장중 arm 거부**(08:50 경로 전용 — 장중 arm은 즉흥 매매 경로). 둘 다 rc=3.
- 흐름 관측치: `.runtime/flow/<YYYYMMDD>.json` 주입 파일만(숫자 외 드롭). **파일 없음=빈 스냅샷=전부 비활성** — OPEN_QUESTIONS **SEL-1**(🟡, NXT 어댑터 부재) 등록.
- **arm 워크플로(§6 의도된 마찰)**: 활성 판정돼도 초안 status가 **approved일 때만** `armed` 새 version append. draft(미승인)는 "arm 보류" 보고만. arm 시 **P1 알림**(§8: 플레이북 arm — "저녁 보고에서 집행 편차 검토", 21:00 기한).
- 배선: `trading.run select-playbooks` + cron `select-am`(08:50) — 15개 잡, disabled 정책 유지.

## 검증
- pytest **253 passed**(+11), mypy **0 issues (93 files)**. 선택기 경로 LLM import 없음(grep 확인 — 절대금지 #2 준수).
- M3 AC: 조건 불일치 → 비거래 ✅ / 관측치 없음 → 비활성 ✅ / approved만 arm + P1 ✅ / 미승인 보류 ✅ / 장중·휴장 거부 ✅ / 화이트리스트는 계약 레벨(test_r5) ✅.
- 실거동: 당일 플레이북 없음 → "비거래" (전일 R5가 비거래 선택했으므로 정합).

## 남은 것
- R6 보고(모닝 06:50/저녁 21:00) — 남은 M3 마지막 큰 조각. 모닝이 R5.5 결과(arm 상태)를 렌더.
- SEL-1: NXT 어댑터 확정 시 스냅샷 생성만 추가(선택기 무변경).
