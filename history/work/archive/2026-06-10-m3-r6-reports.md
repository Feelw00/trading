# 2026-06-10 — M3: R6 보고 (모닝 06:50 / 저녁 결재 21:00 — 정적 렌더, 설계서 §3 R6·§8)

## 산출
- **템플릿 2종** (`reports/templates/*.md.j2`, Jinja2 `StrictUndefined`):
  - `morning`: 읽기 전용 명시, 간밤 거시(as_of 병기)·오늘 플레이북/주문 status·관측 체크리스트. 플레이북 없으면 **"오늘 할 일 없음 — 비거래"**(정상 결론).
  - `evening`: 의사결정 문서 — 집행 편차/보유 무효화 거리/수급(셋 다 어댑터 미구현 → **결측 명시, 추측 대체 없음**), R4 생존/기각 요약(as_of), R5 시나리오, **내일 OrderDraft 승인 요청(draft 상태만)** + "승인은 수동(의도된 마찰)" 안내, P2 알림(당일).
- **렌더** (`reports/render.py`): PlaybookStore·EventStore·AlertStore·거시 landing에서 결정론 조립(LLM 없음). **분량 가드 `MAX_REPORT_CHARS=7000`**(§8 5분 독해, knob) — 초과 시 자동 축약 아니라 `ReportLengthError`(M4 지시).
- **러너** (`report.py`): 렌더 → `.runtime/reports/<일자>-<종류>.md` 저장(원본) → Telegram 발송(절단은 채널 몫, 실패 시 파일 보존+로그). 분량 가드 실패 → **P1 알림**("상류 산출 점검", 다음 슬롯 전).
- 배선: `trading.run report-morning|report-evening`, cron `report-am`(06:50)·`report-pm`(21:00) — **17개 잡** 전부 disabled 정책 유지. jinja2 의존성 추가(설계 명시 스택).

## 검증
- pytest **259 passed**(+6), mypy **0 issues (96 files)**.
- M4 AC 선행: 분량 가드 테스트(초과=실패+P1, 축약 없음) ✅ / as_of 병기 ✅ / 결측 명시 ✅ / approved 초안은 승인 요청 목록에서 제외 ✅.
- **실거동**: 저녁 결재 보고 1,420자 → 파일 + **Telegram 실발송**. 실데이터 구성 확인 — R4 18건(생존0/기각18), R5 카카오뱅크 시나리오 트리, 미수집 3섹션 결측 명시, "승인 요청 없음 — 내일 비거래".

## 남은 것 / 발견
- 시나리오 as_of가 비거래 런에서 "(no-playbook)"으로 표기 — 코스메틱, synth_runs.as_of에 날짜를 항상 넣도록 후속 정리 후보.
- R7(주간 평가·캘리브레이션) — M3 마지막 라운드.
- 승인 전이(draft→approved) 운영자 도구 — 현재 수동(store API). R6 보고에 안내만.
- cron 일괄 enable 결정 — R0~R6 전 슬롯 등록 완료, 운영자 판단 대기.
