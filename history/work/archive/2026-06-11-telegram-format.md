# 2026-06-11 — R6 보고 가독성 재설계 + Telegram HTML 서식 통일 (운영자 피드백 세션)

## 1. 저녁 결재 보고 재설계 (커밋 5cae137)
피드백: "가독성이 최악" — 결정이 맨 아래, 빈 결측 3섹션, R4 단어 중간 절단, 시나리오 통벽.
- **결정 우선**: "내일 OrderDraft 승인 요청"을 문서 최상단(하루 유일 판단 시점이 첫 줄).
- 결측 3섹션(집행/포지션/수급) → 말미 "데이터 한계" 1곳 통합(결측 명시 원칙 유지, 어댑터 생기면 본문 복귀).
- 시나리오: `[분기X]`·`결론:` 마커 결정론 재배열(내용 무변경) — 종목 헤더 + 분기 불릿 + 결론 강조 (`render._scenario_lines`).
- R4: 생존 전부 + 기각 강도 상위 5만, 단어 경계 80자 절단(`_clip`).
- 부수: 비거래 런 synth as_of "(no-playbook)" 근본 수정(러너가 합성 일자 항상 기록). 1,450자→1,149자.

## 2. Telegram HTML 서식 (커밋 798acd5 + 92e8572)
피드백: "텔레그램은 md를 지원하지 않아" — `#`/`**`/`-` 기호 그대로 노출.
- **`reports/tgfmt.to_telegram_html`**: R6 템플릿 서브셋 전용 결정론 변환 — 헤딩/`**`→`<b>`, `>`→`<i>`, `- `→`•`, `- [ ]`→`□`, 본문 전체 HTML 이스케이프(LLM 산출 `<`·`&` 안전), 연속 빈 줄 압축.
- `TelegramChannel.parse_mode` 옵션 필드(기본 None=평문). 보고 러너: HTML 발송 → 거부 시 평문 폴백. 파일은 md 원본 유지.
- **알림 동일 적용**: `format_alert_html`(등급·what·행동 굵게, 전 필드 이스케이프) + `dispatch._deliver`(P0 즉시·P1 다이제스트 양 경로, HTML 거부 시 평문 폴백 — 알림 미달이 최악).
- 실발송 검증: 저녁 보고·모닝 브리핑·P0 테스트 알림 전부 HTML로 재발송 확인.

## 검증
pytest **272 passed** (267→272), mypy **0 issues (103 files)**.

## 비고
- 같은 날 오전의 drill.py + 트리거 3단 진화는 별도 archive: `2026-06-11-first-auto-cycle-audit.md`.
- 다음 자동 슬롯부터(12:30 digest, 16:05~ pm 사이클, 06:50 모닝) 새 서식·새 아키텍처로 발송.
