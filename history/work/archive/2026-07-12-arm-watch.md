# 2026-07-12 — 장중 발동 감시기 (watch/arm_watch) + cron 18슬롯

## 배경 (운영자 2026-07-12)
발동 조건(전일고가 회복·체결강도·호가)은 장중 아무 때나 충족되는데 아침 arm-check는 1회
스냅샷 — 조건 감시가 운영자 몫으로 남아 "계속 보고 있어야 한다"(P-9 출발점)와 충돌.
운영자 거래 창은 09:00~15:00. M4 이벤트 감시기 골격의 첫 인스턴스.

## 산출
- **`watch/arm_watch.py`** (순수 코드, LLM 미개입):
  - `run_pass`: 감시 창(거래일 09:00~15:00) 가드(rc=3) → **arm-check와 동일 판정 경로 재사용**
    (`assess` → flowsnap(KIS 실시간) → selector.engine) → 활성 approved 초안의 조건 전원 충족 시
    **P0 즉시 발화**("발동 — 종목 매수: 조건 충족 (충족 조건 나열)" / 행동=지정가+손절 동시 입력 /
    기한=15:00 전). **미승인 후보는 감시 안 함**(소음 방지).
  - **WatchStore**(`data/watch.sqlite`, append-only): 초안·일자당 1회 dedup — 재기동에도 유지.
  - **마감 전 정리 리마인더**(14:40~, 1회): 활성 풀 존재 시 "미체결 예약 취소·손절 확인" P0.
  - `run_loop`: 세션 내 90초 폴링, 15:00 자기 종료. **하트비트 파일**(5분 신선도)로 중복 기동 차단.
- **배선**: `trading.run`에 `arm-watch` 라운드, cron 2슬롯 신설(09:00 기동 + 12:00 재기동
  안전망) → sync 적용, 게이트웨이 18잡 확인. 기동은 전적으로 openclaw cron(자체 스케줄러 아님) —
  README "예정"이던 heartbeat 배선의 실현체(openclaw heartbeat CLI 검증 후 이관 가능).
- `python -m trading.watch`(루프) / `--once`(1패스 — 드릴 검증용, 세션 밖도 rc 0).

## 운영 프로토콜 (감시기 + 3터치)
① 9:00~9:30 arm-check 1회(즉시 발동분 진입) ② 미발동분은 감시기가 P0 알림 → 알림 시
1~2분 확인 후 지정가+손절 입력 ③ 14:40 감시기 리마인더 → 미체결 정리. 장중 상시 관찰 불필요.

## 검증
- 테스트 6건: 세션 가드(일요일·개장 전·15:00 경계)·1회 발화+dedup·비활성 무발화·
  마감 리마인더 1회·빈 풀 무알림·하트비트 중복 기동 차단. 전체 스위트 135건 + mypy strict.
- 실 엔트리포인트 `--once` 검증(일요일 → 세션 밖 스킵 정상).

## 남은 것
- 서킷브레이커·환율 임계·바이너리 전이 감시(P0) — 같은 골격에 패스 추가(M4 잔여).
- KIS 폴링 비용: 초안 4건 × 90초 = 시간당 ~160콜 — KIS 한도 내이나 초안 수 급증 시 간격 조정.
- 월요일(7/13) 첫 실전: 09:00 기동 로그(`.runtime/logs/cron/arm-watch.log`) 확인.
