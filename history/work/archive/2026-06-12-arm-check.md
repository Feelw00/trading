# 2026-06-12 — arm-check: 아침 발동 판단 + 결정론 해설 + LLM 분석 (P-6)

운영자 피드백(6/12): 텔레그램 결재 보고를 봐도 "이 OrderDraft가 지금 발동했나, 무슨 뜻이냐"를
매번 사람이 코드를 뒤져 설명해야 한다. 9~10시 집행 시점에 **관련 정보 수집 → 발동 트리거 판단 →
해설 + LLM 분석**을 한 번에 받는 온디맨드 시스템 요청. 운영자 결정 "KIS 실시간 + 골격 동시".

## 헌법 제약 분리

- **발동 판단 = 순수 코드**(절대금지 #2: R5.5 선택기에 LLM 금지) — 기존 `selector/engine.py` 재사용.
- **LLM은 해설만** — 판단(ACTIVE/INACTIVE) 끝낸 뒤 리스크·맥락 분석(discuss 패턴: 결정론 grounding→LLM).
- **데이터 소스 추측 금지**(절대금지 #1) — KIS 실시간 TR은 장중 실호출로 필드 관측 확정.

## 산출

- **`reports/explain.py`(신규)**: 흐름변수→한국어, 3트랜치 진입 구조, 스탑/시간손절, cap 표현식
  결정론 해설. FLOW_VARIABLES 해설 누락은 import 시점 RuntimeError 가드.
- **`collectors/kis.py`**: 실시간 시세성 TR 2종 추가. `quote_ccnl`(inquire-ccnl FHKST01010300 —
  현재가 stck_prpr·체결강도 tday_rltv), `quote_asking_price`(inquire-asking-price-exp-ccn
  FHKST01010200 — total_bidp_rsqn·total_askp_rsqn). **장중 실호출 관측 확정**.
- **`flowsnap.py`(신규)**: KIS 실시간 + market.sqlite 전일고가 → FlowSnapshot. 주입 파일
  `.runtime/flow/<날짜>.json`을 베이스로 KIS 실시간이 덮어씀. 결측·비수치는 미수집(추측 금지).
- **`arm_check.py`(신규)**: approved/draft 로드 → flowsnap → selector.select() 순수 판단 →
  결정론 해설(AssessResult/render_text). **arm 안 함**(읽기 전용, 집행은 운영자 수동). 장중 거부 없음.
- **`.claude/skills/arm-check/SKILL.md`(신규)**: LLM이 arm_check 산출을 받아 4단계 분석(미충족 의미·
  평가불가 경고·집행 주의·결측 정직).

## 실호출 검증 (2026-06-12 장중, 6/11 플레이북 3종)

- **orderbook_imbalance** 타이거일렉 0.146 관측 → 호가 TR 필드명 확정.
- **execution_strength**: inquire-price엔 cttr 없음 발견 → inquire-ccnl `tday_rltv`로 확정
  (피에스케이 91.96 < 110 → 정확히 미발동). 종목당 2콜(ccnl+호가).
- **prev_day_high_reclaim**: 현재가>전고 → 1.0 관측되나 R5가 낸 `==true` 조건을 selector가
  평가 못 함(숫자 문법만) → 평가 불가 노출. **SEL-2로 등록**(R5↔selector 계약 틈, P-6 범위 밖).

## OPEN_QUESTIONS

- SEL-1 🟡 **부분 해소** — KIS 실시간으로 3변수 충족, premkt_volume_ratio·gap만 NXT 🔴 잔존.
- KIS-RT-1 🟢 **관측 확정** — 실시간 TR 필드 실호출 검증.
- SEL-2 🟡 **신규** — boolean 흐름변수(`==true`) vs selector 숫자 문법 불일치.

## 검증

- pytest **316 passed**, mypy strict **0 issues (76 files)**.
- 신규 테스트: explain(조건·트랜치·cap·헤드라인·평가불가)·flowsnap(빈/주입/KIS계산/덮어쓰기)·
  arm_check(비거래·미발동사유·주입발동·장중표기)·KIS TR(ccnl [0]행·호가 output1·빈 안전).
- 실데이터(6/11 플레이북) + KIS 실시간으로 render_text 육안 확인.

## 잔여

- SEL-2: `==true` 평가 — R5 프롬프트 또는 selector 변경(별도 결정).
- premkt_volume_ratio: NXT 소스(🔴) — arm-check는 KIS 가용분으로 부분 충족.
- arm-check cron 미등록(온디맨드 전용, `python -m trading.arm_check` 직접 실행).
