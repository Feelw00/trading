# 2026-08-31 — P-18 구현: 우선순위 반전 — 가치 코어 게이트, 사이클은 도구

## 경위
- 운영자 언명: "지금 사이클이라는 단어에 너무 편향되었어. **핵심은 가치투자고 사이클은
  도구에 불과해**." (같은 흐름: "화이트리스트가 너무 적어" · "방산은 사이클 산업")
- 실측 근거: 직전 산출에서 가치·건전성 전부 통과 후 **오직 "발동 존 아님"만으로 탈락 34종**
  (통과 0). 화이트리스트 밖 저평가 우량주는 심사대에 오르지도 못했음.
- P-18 기안(방향 승인) → 세부 결재 3항: **① 과열 = 플래그만 달고 편입 ② 유니버스 = 전 상장
  ③ 가치 기준 = 산업 내·시장 전체 병행** → 같은 날 구현.

## 인코딩 (설계서 §3 R4 개정 주석 · POLICY_PARAMS §5 v1.6 · OPEN_QUESTIONS PIVOT-11 🟢)
- **게이트 = 가치·건전성만**(`screen/rules.py`): 가치 병행(산업 내 PBR ≤40% **OR** 시장 전체
  ≤40% — 시장 임계는 기결재 40% 미러링, 세션 해석 에코백) + 생존력 + 가치 함정 방어(v1.2)
  + 구조적 사양 가드 + 하드 탈락. **발동 존 검사 폐지.** 금융 부채 면제를 버킷 산업명
  (금융·보험)까지 확장.
- **사이클 = 도구**(`screen/run.py`): 국면·`cycle_caution`(과열 ⚠) 레코드 박제, 후보 정렬
  = 바닥·회복 산업 우선 → 시장 percentile. 국면 레코드 부재 산업도 심사(unknown 정직,
  `cycle_ref=None`). R5 사이징 가중은 §6 결재에 위임.
- **2패스 심사**: 화이트리스트 큐레이션 산업(다중 소속 산업별 각각) + 나머지 전 종목 KRX 버킷.
  미분류 버킷은 산업 percentile 미산출(시장 기준만). 후보 id에 산업 포함(다중 소속 레코드
  가림 방지), 회차 as_of 단일화(배치 조회 정합).
- **유니버스 확장**: `collectors/fins.py` = 전 상장 시총 내림차순(+큐레이션 안전망),
  `sectors.py` KRX 태깅 = 전 상장(KIS bstp — grounded/KSIC 보강은 기존 스코프 유지, DART
  한도 보호). `MarketStore.names_on` 신설. `CandidateRecord`에 `market_pbr_pct`·
  `cycle_caution` 추가, `cycle_ref` optional.
- **보고 재편**: 다이제스트(md·HTML)·대시보드 — "가치 후보(N, 과열 ⚠ M)" 표(우선순위 정렬·
  표시 상한 40/60/10) + 신규 후보만 심사 패킷 카드. 온도계 HTML도 심사 대상/관찰 이원화.
  대시보드 카드2 "진입 존"→"우선순위 존"(게이트 아님 명시).

## 실행 결과 (2026-08-31 밤)
- 재무: 전 상장 2,876 대상 · 확보 2,663 · 연간 10년 백필 17,721건 → **재무 DB 420→2,669종목**
  (DART 한도 초과 없음). 밸류에이션 2,669(PBR 2,651·섹터상대 2,611).
- 업종 태깅: KIS bstp 신규 2,465(총 2,745), 스킵 130은 일간 체인 재시도.
- **R3: 31→38개 산업**(전 시장 태깅으로 섬유·의류·종이·목재·출판 등 버킷 성립).
  국면 분포 바닥 6·하강 9·회복 5·과열 12·불가 6 — 버킷 구성이 전 시장으로 재편되어
  관찰층 판정이 정직해짐(화이트리스트 큐레이션 9은 불변).
- **R4: 평가 2,672 · 통과 546 (과열 ⚠ 235)** — 직전 187/0에서 전환. 탈락 분포가 가치
  필터 중심으로 재편: 가치 미달 1,395 · 만성 저수익 1,106 · 적자 상한 1,058 · 함정 방어
  993 · … · 구조적 사양 12. **"발동 존 아님" 사유 소멸.**
- 상위 후보 = 바닥 통과 존(화학·IT서비스·오락문화·제약 버킷)의 저PBR + 초저PBR 지주
  (KG케미칼 0.07배 등 — 연결자본 지주 디스카운트 아티팩트, 아래 잔여 참조).
- 웹 재기동 — 대시보드 "가치 후보 (546, 과열 ⚠ 235)" 정합. pytest 전체 그린·mypy strict 클린.

## 잔여·후속
- **지주 디스카운트 아티팩트**: 연결자본 기반 PBR로 지주가 시장 최저 percentile 상시 점유 —
  전량 편입 대상이 됨. 별도 처리(지주 태그 감점/분리)는 정책 결재 사항 — R7 안건 후보.
- **관리종목·감사의견 필터 미적용(PIVOT-3)**: 전 상장 확장으로 위험 노출이 커짐 — 우선순위 상향 권고.
- 태깅 스킵 130 재시도(일간 자동) · 시장 percentile 임계 재캘리브레이션(R7) ·
  R5 사이징의 사이클 가중 계수(§6 결재) · 미분류 잔여(태깅 실패분)의 시장 기준 단독 심사 유지.
- eod-v3 일간 재무 자연 갱신이 전 상장 스코프로 확대 — DART 호출량 관찰(멱등이라 정상시 소량).
