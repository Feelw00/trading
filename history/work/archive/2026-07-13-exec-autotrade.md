# 2026-07-13 — EXEC-1: 자동 집행 전환 (운영자는 보고만, 시스템이 주문까지)

## 결정 (운영자, AskUserQuestion 확정)
- §6 수동 결재·수동 집행(의도된 마찰) → **자동 승인+거부권 / 자동 집행** 전환.
- 파라미터: 하드캡 **종목당 100만·총 500만·동시 5종목·일 2건** / **dry-run 5거래일** 후
  운영자가 live 전환 / 브로커=토스증권 Open API(키 발급됨). 상세: OPEN_QUESTIONS EXEC-1.

## 산출
- `collectors/toss.py` — OAuth2(form-urlencoded)+파일 토큰캐시, 계좌/보유/현재가(200배치),
  **지정가 주문 + 조건부 청산(SINGLE·SELL)**. orderType=LIMIT 하드코딩 — 시장가 경로 부재
  (모듈 소스에 "MARKET" 문자열 부재를 회귀 테스트로 가드, 절대금지 #3).
- `executor.py` — ExecPolicy(env 하향만)·ExecStore(append-only `data/exec.sqlite`)·
  `execute_armed`(캡→즉시 트랜치 비중만 지정가 매수, dry-run은 기록+P0만)·
  `reconcile`(체결 폴링→손절 조건주문[트리거 2틱 아래 지정가]→PositionRecord 자동 박제).
  KRX 호가단위 표 내장. 킬 스위치 `.runtime/exec/KILL`.
- `watch/arm_watch.py` — `execution_pass`를 감시 패스에 연결(발동→집행, 매 패스 체결 추적).
  가격: 토스(주문 브로커 동일 소스)→KIS 폴백, 결측 시 스킵. 집행 오류는 감시를 죽이지 않음.
- `run.py` — synth 성공 직후 **자동 승인**(`approve.auto_approve_pending`) + P0 통지
  (거부 안내 `--veto <id>`, 기한=다음 거래일 09:00). `contracts/order.py`에 VETOED 상태 추가.
- `.env.example`(TOSS_*·EXEC_*), SECRETS.md 2b(키·IP·회전), OPEN_QUESTIONS EXEC-1 🟢.

## 검증
- pytest 전체 통과(신규: toss 8건 — 토큰캐시·envelope·LIMIT 본문·계좌헤더·batch 상한·
  시장가 부재 가드 / executor 11건 — 틱·캡 하향만·킬스위치·dry-run·일일/종목/동시 캡·
  체결→스탑→포지션·미체결 대기·시간손절 전용·자동승인/veto 전이). mypy strict 0 (132 files).
- 실거동: `trading.run arm-watch` 세션 밖 rc=3 + 모드 배너 정상.

## 운영 시작
- **오늘 밤 21:05 synth-pm부터 자동 승인 가동**(EXEC_MODE 미설정=dry-run 기본).
- **dry-run D1=2026-07-14(화)** → 5거래일(7/17 휴장 제외, ~7/21 월) 후 live 판단.
- 운영자 잔여 작업: `.env`에 TOSS_CLIENT_ID/SECRET 주입 + 허용 IP 등록 + accounts()로
  TOSS_ACCOUNT_SEQ 확인·기입. (키 없어도 dry-run은 KIS 가격으로 동작.)

## 후속
- `/approve` 스킬 문구가 구세계(수동 결재) — veto 중심으로 개정 필요.
- 확인(condition) 트랜치 v1 미집행 — 후속 슬라이스. 임의 청산(스탑 외) 자동화 없음.
- 갭다운 시 지정가 손절 미체결 위험 — 지정가-only 제약의 본질, dry-run 기간 관찰.
- P-10(감시기 기동 알림)과 결합하면 "오늘 자동 집행 대상 N건" 아침 통지 완성.

## 2차 개정 (같은 날 저녁 — 운영자 논의)
- **투자 정책 캡 제거**: 계좌 500만 고정 예치=자연 상한. 사이징 = 가용액(live는 실
  매수가능금액 `buying_power_krw` 신설) × R5 cap 계수(cap_fraction — "0.5 * normal_unit"
  파싱, 해석불가/범위밖=0.5 보수) × 즉시 트랜치 비중. **최소 1주 보장**(고가 종목 배제 방지).
  같은 종목 재진입(새 초안)=계단식 허용. 잔존 가드: 일일 신규 5건(폭주 방어)·dedup·킬스위치.
- **미국 주식**: KR live 검증 후 별도 마일스톤(운영자) — 토스 US 지원(LOC 포함) 확인됨.
- 검증: 테스트 개정(사이징 24주 계산·계단식 허용·최소 1주·잔고 부족 스킵·cap_fraction 4케이스)
  전체 통과 + mypy 0.

## 3차 — P-11 Stage B (같은 날 밤)
- **OCO 익절**: reconcile이 체결 시 손절+익절 OCO 등록(익절=결정론 R:R 1.5, `EXEC_RR`).
  `toss.place_oco_sell`(익절 트리거≤손절 차단) · 체결가≤손절이면 SINGLE 폴백.
- **R5 초안 풀 확대**: max_playbooks 5→8 + '조건 까다로운 대안 셋업' 프롬프트(빈 배열 규율 유지).
- **sector_ignition 흐름변수**: FLOW_VARIABLES·OBSERVABLE_FLOW_DESC·explain 등록.
  flowsnap이 토스 실시간 거래대금 상위 100 × stock_sectors(KRX 업종) 조인, 동일 업종 ≥5 = 점화.
  라이브 검증: 전기·전자/금융 1.0, 유통 0.0 (20:40 실호출).
- 검증: 신규 테스트(OCO 본문·트리거 순서 가드·랭킹·점화 스냅샷 2건·기존 4건 토스 차단 패치)
  전체 통과 + mypy 0. 오늘 밤 R5(21:05)부터 새 프롬프트·새 변수로 산출.

## 4차 — EXEC-2 계단식 청산 (같은 날 밤, R5 21:05 전 배포)
- 운영자 제안(계단 매도) 논의 → 하방은 "경고 축소+하드 전량"으로 수정 확정(무효화 규율 유지),
  익절 레벨은 R5 분석 지정, 첫 익절 후 본전 상향.
- 계약 targets/soft_stop(선택) + R5 프롬프트/파싱 + 브래킷 상주(OCO 최종타깃) +
  `manage_exits`(부분 레그·브래킷 취소→재등록·본전 상향) + toss DELETE.
- 테스트: 사다리 4건(타깃 레그+본전 상향·경고 축소·레벨 사이 무동작·최종타깃 브래킷) 포함
  전체 통과 + mypy 0. R5 프롬프트 변경은 21:05 첫 산출부터 유효.

## 5차 — EXEC-3 애프터 청산 전용 감시 + 레그 체결 확인 (같은 날 밤 2)
- 운영자 결정: 애프터(NXT ~20:00) **청산만, 진입 금지**. 감시 창 이원화(진입 15:00 / 청산 20:00),
  15:00~15:30 공백 해소. cron `arm-watch-exit`(15:35 안전망) 등록 — 19잡, sync 적용 완료.
- 부분 레그 체결 확인 v1.1: orderId 추적→폴링→leg_fill 박제 / 호가 이탈 시 취소·재호가 /
  거부·취소는 P1. 테스트(청산 전용 창·20:00 마감·전 스위트) + mypy 0.

## 6차 — EXEC-4 가격 상한 + 갈아타기 (같은 날 밤 3)
- 주당 200만 초과 배제(계단 청산 보전) + rotation(잔고 부족 시 열등 포지션 매각→재진입).
  가드레일: 러너 보호·손절 근접=잔여가치 0·교체 마진(2배+2%p)·일 1회.
- 버그 수정: 잔고 부족 발동분 영구 미집행 → 매 패스 재시도. dry-run 잔고에 교체 대금 반영.
- 테스트: 가격 상한·교체 성사(마진 충족)·러너/마진 거부·일 1회 — 전체 통과 + mypy 0.

## 7차 — 첫 실산출 관측·보정 (21:06 synth 직후)
- **신 체계 첫 실산출 성공**: 초안 7건 전부 사다리 완비(soft_stop+targets, 2단 타깃 2건,
  time_stop 7~12일 가변). 내일 활성 풀 = 오늘 7건.
- **보정 3건**:
  ① 자동 승인이 과거 미승인 잔재(6/11~7/12, 12건)까지 부활 → **당일 생성분 한정**으로 수정
     + 7/12 활성분 4건 veto 원상복구(6월분은 TTL 만료라 무해).
  ② **셋업 붕괴 가드 신설**: 현재가 ≤ 손절 레벨이면 진입 금지 — 실사례: 데이타솔루션 초안
     stop 6,500 vs 당일 종가 6,130(R5는 T-1 EOD 기준이라 익일 급락 시 전제 붕괴).
  ③ (6차의 재시도·rotation과 결합 검증) 전체 스위트 + mypy 0.

## 8차 — 자동 승인 P0 다이제스트화 (운영자 피드백: 기계 ID 나열 금지)
- 21:06 P0가 원시 id 19건 나열 + 과거 잔재 포함 — 형식·내용 이중 결함.
- 내용은 7차에서 기수정(당일 한정+veto). 형식: `_approved_digest` — 종목명·손절/경고/익절
  사다리·시계 표기(텔레그램 결재 다이제스트 원칙). 실데이터 미리보기 검증.

## 9차 — EXEC-5 풀 비례 배분 (운영자 지적: 발동 순서가 배분을 정하면 안 됨)
- 배분 = 계좌 기준액 × 계수/풀 계수합(당일 고정) → 순서 불변(테스트 증명). 잔고 상한 별도.
- 계수 균등(현 0.5)=균등 분할, R5 계수 차등 도입 시 확신 가중으로 무변경 진화.

## 10차 — EXEC-5 개정 + EXEC-6 부분 트림 (운영자 재설계: 자본 가동률 우선)
- 분모를 고정 풀→**미집행 잔여 풀**로(남은 트리거 적으면 몫 증가), 기준=가용 잔고.
- 잔고 부족 회수 사다리: 갈아타기(전량)→부분 트림(부족분만, 러너·손절근접 제외,
  포지션당 50% 상한, 브래킷 잔량 교체)→재시도. committed에 trim_sell 반영.
- 테스트: 동적 몫·트림 충당·50% 상한·러너 보호 — 전체 통과 + mypy 0.

## 11차 — 7/13 폭락 실검증·대응 (21:30 저녁 보고 검토)
- **실검증: 코스피 -8.9%(6,806.93) 폭락 사실** — R4가 기각한 급락 뉴스들이 전부 진실
  (적대검증 거짓 음성 — 후속 조사 필요). 풀 7종 대조: 데이타솔루션 -16%(하방 가드 차단),
  **한국콜마 +11.5%로 익절 레벨 초과** → **셋업 소진 가드 신설**(현재가≥익절1 진입 금지).
- 저녁 보고 "내일 비거래" 오보 수정: 자동 승인 체제 반영 — approved 풀을 "내일 감시 풀"로
  표시(다이제스트 헤더 "자동 승인 N건"), 템플릿·테스트 정합.
- 잔여: R4 시장 급락(극단 사건) 뉴스 거짓 음성 원인 조사 — 다음 세션 최우선.

## 12차 — EXEC-7 레짐 채널 + R4 원인 확정 (운영자: "모두 진행")
- 원인 확정: R4는 정당("사후 보도=촉매 부적격") — 결함은 레짐 전용 채널 부재 + R5 입력 T-1 한계.
- A. regime.py: 실시간 지수 vs 전일 종가 → CAUTION(-3%, 배분 절반)/RISK_OFF(-5%, 신규 중단)/
  UNKNOWN(보수). 감시 패스 배선 + 하루 1회 P0. 라이브: 당일 -8.9% RISK_OFF 정확 판정.
- B. live_backdrop_lines를 R5 합성·R6 보고에 주입(EOD 지연 보정).
- C. 보고 기각 라벨 "촉매 부적격 포함 — 사실 판정 아님".
- D1 계측: EXEC_TEST_ENTRY=1 — 11시까지 발동 0이면 최고 R:R 초안 1주 관통(임시).
- 풀 재검토(실시간): 유효 브이엠·티에스이·뉴파워 / 소진 테스·한국콜마 / 붕괴 데이타솔루션.
