# 2026-08-29~31 — P-16 웹 가시성 개편 (V1·V2·V3) + 포트 80 도메인 접속

운영자 피드백(2026-08-30): "대시보드 확인 가능한 데이터가 너무 적고 가로 막대그래프뿐,
호버 등 편의 기능 전무. PBR·ROE 중앙값이 뭔지 알 수 없음. R4는 통과가 중요한데 찾기 힘듦.
국면 색 구분 필요. 보고서는 없느니만 못함." → V1/V2/V3 3단계 계획 승인 후 순차 구현.

## 접속 경로 (선행 작업, 2026-08-30)
- 운영자 도메인 → 테일넷 IP(100.127.142.59) 연결, 포트 라우팅 불가 → 기본 포트 80 채택.
- macOS 비루트 특성: 특정 IP 저포트 바인딩 거부, 와일드카드(0.0.0.0)는 허용.
  tailscale serve는 ts.net Host만 응답해 임의 도메인 불가 → serve reset 후 `0.0.0.0:80`.
  LAN 노출 확대 고지(읽기 전용 사이트라 수용). `ops/start-report-site.sh` PORT 기본 80.

## V1 — 정보 위계·용어·색 (커밋 94563d0)
- `web/glossary.py` 신설 — 지표 17종 호버 설명 단일 소스(`tip()`), 국면 5색 배지
  (`phase_pill()`: 바닥 통과=초록·회복=파랑·과열=빨강·하강=주황·판정 불가=회색) 전 페이지 공통.
- 대시보드 재구성: 핵심 카드 3장(통과 종목 크게 / 진입 존 산업 / 직전 산출 대비 변화 —
  국면 전환 + 후보 진입·이탈 감지 `passed_delta()` 신설), 신선도는 `<details>` 접기.
- 종목: R4 통과 테이블 상단 분리 + 통과 행 하이라이트 + 산업 필터 셀렉트 + 헤더 전부 툴팁,
  적자 표기 "1년/5년" 시맨틱화.

## V2 — 차트 편의·다양화 (커밋 7ab9028)
- SVG 네이티브 `<title>` 툴팁 전면(무의존): 온도 지도 바·깔때기 단계(거르는 기준 설명)·
  라인 포인트(날짜: 값)·이중 바(연도 시리즈: 값)·국면 띠 셀.
- 라인 차트: 포인트 원(최종점 강조)·labels 세로 눈금. 주가 다운샘플을 (날짜, 값) 쌍으로
  개정해 라벨 보존. 수급·공매도·대차·섹터 PBR·마진 차트에 날짜/연도 라벨 배선.
- 대시보드 "산업 한눈에": 국면 분포 도넛(`donut_chart` — 조각 툴팁·중앙 합계·100% 원 처리) +
  산업 히트맵 타일(색=국면, PBR밴드·온도, 클릭=상세, 저평가 순).

## V3 — 주간 보고서 변화 중심 재설계
- `weekly_digest.render_html` 전면 개정: ① 결론 헤드라인 카드("변화 없음 — 관찰 유지" 또는
  "신규 후보 N · 국면 전환 M") ② 전주 대비 변화(국면 전환 배지 쌍·후보 진입/이탈 종목명)
  ③ 통과 후보 카드(종목명·신규 배지, 심사 패킷은 `<details>` 접기) ④ 온도계(밴드 오름차순·
  진입 존 초록 행·용어 툴팁) ⑤ 탈락 사유 건수 내림차순 ⑥ 미적용 필터 접기.
- 변화 산출: `CycleStore.recent_phases(n=2)` + `web.data.passed_delta()` 재사용(DB 최근
  2회차 비교 — 리포트 생성 시점의 박제 데이터만, 판정 미개입). 정책 버전 하드코딩
  "policy-v1.2" → `POLICY_VERSION` 상수로 교정.
- md 원장(`render`)은 유지 — HTML만 운영자 가독용 재설계. 이번 주분 재생성으로 즉시 반영.

## 검증
- pytest 전체 그린(신규 9건: glossary·대시보드 카드·통과 분리·라인 포인트 툴팁·이중 바
  툴팁·도넛·깔때기 설명·보고서 위계/무변화 헤드라인), mypy strict 186파일 클린.
- 실검증(맥북 ssh → 도메인 경유): 전 라우트 200, 상세 차트 "2026-08-13: 761,348" 툴팁,
  도넛 조각·히트맵 타일·보고서 헤드라인/배지/접기 렌더 확인.

## 남은 것
- 수급 60일 창 도달 시 네거티브 스크린 배선 제안(별개 트랙), §6 결재 대기 동일.
