# 2026-06-10 — M3: R7 평가·캘리브레이션 + 레짐 모니터 (설계서 §3 R7·§7)

## 산출
- **ScoreRecord 계약** (`contracts/score.py`): PersonaScore(채점/미성숙/flat/결측 분리 카운트, hit_rate는 표본 0이면 None — 0% 왜곡 금지) + CalibrationBucket(확신도 구간 vs 적중) + R4 정확도 4카운터 + 레짐 비율 + notes(측정 한계 명시).
- **결정론 채점기** (`rounds/r7.py` — **숫자=코드, LLM=해석만**):
  - 논제: as_of 익영업일 종가 → horizon 거래일 후 종가 방향 일치. **트리거 발동 감지 불가 → "트리거 무관 채점" notes 명시**(R7-1). 미성숙·결측은 채점 안 함(부분 채점 금지).
  - 캘리브레이션: 확신도 4버킷([0,.4)/[.4,.55)/[.55,.7)/[.7,1]) vs 실현 적중.
  - R4 정확도: single_stock 검증 이벤트 — 기각 후 3거래일 |이동|<3%=기각 정확, 생존 후 ≥3%=생존 정확(둘 다 knob).
  - 레짐 프록시: 전종목 |등락률| 중앙값 최근 5일/기준 20일 비율(`MarketStore.daily_change_medians` 신설). 설계 입력(시초 변동성·신용잔고·레버리지 ETF) 미수집 — **R7-1**(🟡) 등록.
- **해석·개정안 (claude -p)**: 코드 계산 성적표만 입력("수치 재계산 금지"), 출력은 `.runtime/reports/<일자>-r7-proposal.md` **박제만 — 자동 적용 금지(운영자 승인 후 버전 태깅)**. 표본 없으면 LLM 호출 자체를 생략. R4 공격 강도 조정 제안 포함(§3 R4 "성적 나쁜 페르소나를 더 가혹하게").
- **ScoreStore** (`journal/scores.py`, append-only) — R4가 읽을 페르소나 성적표 단일 원천.
- 배선: `trading.run evaluate` + cron `eval-sat`(토 10:00) — **18개 잡** 전부 disabled.

## 검증
- pytest **267 passed**(+8), mypy **0 issues (101 files)**.
- 채점 정확성: long 적중(+10%)/short 빗나감/미성숙 보류/flat·결측 분리/캘리브레이션 버킷/R4 정확도(생존-대이동·기각-무이동)/레짐 점프 감지/표본부족=None — 전부 픽스처 검증.
- **실거동**: 논제 3건(6/10)·시세 4일 → 채점 0(미성숙 1·flat 2), R4 0/0(전부 미성숙), 레짐 미산출, **해석 생략(표본 없음 — LLM 호출 안 함)**, `score.20260610 v1` 적재 + Telegram 요약 발송. 표본 부족을 표본 부족이라고 말하는 정직한 경로 확인.

## M3 완결
R0~R7 전 라운드 + alerts + 보고 배선 완료. cron 18개 등록(전부 disabled — 운영자 일괄 enable 결정 대기).

## 남은 것
- R7-1: 흐름 스냅샷(SEL-1 공유)·KIS 체결·금융투자협회(신용잔고) 수집기.
- 데이터 누적 후(시세 2주+) 첫 유효 R7 채점.
- M4: 이벤트 감시기 → P0, 리플레이 회귀(6/2~6/8).
