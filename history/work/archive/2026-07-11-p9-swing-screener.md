# 2026-07-11 — P-9 스윙 스크리너 1단계: 품질 유니버스(4축) + 기회 트리거 + EOD 체인 배선

## 배경
운영자 발의: 현행 스크리너는 고변동 단기 급등주 편향 — 상시 관찰 부담·고위험. 기존 점수는
"단기 열기"로 보존하고, 중장기 스윙은 **"유니버스는 느리게, 기회 포착은 매일"** 2단 구조로
(운영자 정정: 주기 리스트 갱신이 아니라 기회 발생 시 진입). P-1/P-2(섹터 커버리지)가 같은 날 선행.

## 산출
1. **`collectors/fins.py`** — DART `fnlttSinglAcnt` 재무 캐시(`data/fins.sqlite`, append-only).
   - **실호출 확인**(팬오션): 분기보고서 IS는 thstrm=당기분기/frmtrm=**전기 동일분기** → 1콜 YoY.
   - 사다리 수집(3Q→반기→1Q→전년 사업보고서→전년 3Q): 공시 시차를 달력 추측 없이 "빈 응답→다음
     후보"로 흡수. 시도 기록(ok/empty)으로 재호출 차단 — 분기당 1회 자연 갱신.
   - `FinSnapshot`: rev/op YoY(전기 0 이하는 None + 흑자전환 별도 플래그)·영업이익률·부채비율.
     결측은 전부 None(0 폴백 금지). 실수집: **240/243 확보**.
2. **`swing.py`** — 순수 코드(LLM 미개입).
   - **4축**: ①추세 품질(변동성 조정 모멘텀 60/120 + MDD-40% 컷 + 정배열) ②도메인 열기(섹터
     breadth·거래대금 점유율 변화 + R2 이벤트 촉매 — 섹터 소표본<3 제외) ③펀더멘털(YoY 캡 ±100%,
     흑자전환=캡, 부채비율>4x 게이트) ④수급(외인+기관 순매수 20일 누적/거래대금 정규화, 관측<5일 결측).
   - **결측 명시**: 축 미확보는 결측(0 흘리지 않음) — 가용 축 가중 재정규화, 추세 필수+최소 2축,
     커버리지·제외 사유 카운트 출력. **뉴스 stale 가드**: EventStore 최신 as_of가 창(10일) 밖이면
     뉴스축·촉매 트리거 자체를 제외(6/15~7/10 공백을 "촉매 없음"으로 오독 방지 — CURRENT ③ 해소).
   - **기회 트리거**(유니버스 & 점수≥0.55): pullback(20일 고점 -3~-15% & ma20 위 & 추세 양) /
     domain_ignition(열기 백분위≥0.80) / catalyst(3일 내 강도≥0.6) / flow_turn(5일 양전 & 20일 음).
     임계 전부 SwingConfig 보수 초기값 — R7 튜닝 예정.
   - **SwingStore**(`data/swing.sqlite`, append-only): 유니버스 스냅샷+트리거 이력 — R6/R7 소비용.
3. **배선**: `trading.run`에 sector-llm/collect-fins/swing 라운드 등록, `daily-eod` 체인 확장:
   전종목 → 섹터(grounded→LLM 폴백 best-effort) → 스크리너 → 수급·재무·스윙(best-effort, 실패 P1) → factpack.
   cron 슬롯 변화 없음(동일 라운드명). 섹터 LLM 폴백은 §5 휴면 비대상(분류 메타데이터, 매매 판단 아님) 판단 — 주석 명기.

## 첫 실행 (as_of=20260709)
- 게이트 243 → 점수 산출 90. 제외: MDD 127 · 부채비율 21 · 추세결측 5. 커버리지: 재무 87/90 ·
  도메인 83/90 · **수급 26/90**(기존 수집 범위 한계 — 알려진 미결).
- Top: 에이피알 0.85 · SK 0.84 · SK스퀘어 0.84 · 한국콜마 0.80 — 급등 스파이크(금호건설 등)는
  MDD·변동성 페널티로 자연 배제됨(설계 의도 확인).
- 기회 트리거 11종: 화장품·지주 도메인 점화 중심 + GS pullback. 첫날 점화 다발은 임계 0.80의
  초기값 특성 — R7 채점으로 조정.

## 검증
- 신규 테스트 25건: fins 파싱(실측 픽스처)·스냅샷 선택·사다리 수집·시도 기록 / 변동성 조정
  모멘텀(완만>스파이크)·MDD·축 결측·흑자전환·YoY 캡·트리거 4종 발화/비발화. 전체 스위트+mypy strict 통과.

## 남은 것 (P-9 2단계)
- **R6 저녁 보고 "스윙 기회" 섹션** — SwingStore.triggers_on 소비(승격 후보 → 결재 노출).
- **R7 채점 축**: 트리거 적중률(발화 후 N일 수익) → 임계·가중치 튜닝.
- **수급 커버리지 확대**(26/90): flows 수집 범위를 스윙 유니버스로 확장(KIS 호출량 절충).
- MDD -40% 컷이 게이트 통과의 52%를 제외 — 2026 강세장 특성인지 과도인지 R7로 판단.
- 삼성전기類 버킷 부재(전자부품·MLCC) — P-1 후속 buckets 검토.
