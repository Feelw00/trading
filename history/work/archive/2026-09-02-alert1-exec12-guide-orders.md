# 2026-09-02 — ALERT-1 실행 보고 · EXEC-12 가이드 매도 예약(live) · /paper·/picks 재구성

> 세션 커밋 13건(`f43d80f`~`178bb10`), 22파일 +1,978/−199. 게이트: mypy 208파일 clean · pytest 전체 통과.
> 운영자 답변은 분석·판정·대안 제시 후 반영(2026-08-26 지시 준수). 지시 에코백은 §5.

## 1. 부팅·상태 확인
- 미커밋 2파일(가이드 비중 % 표기) 커밋, tests 4파일 mypy strict 어노테이션 5건 복구(직전 세션은 src만 검사).
- eod-v3 cron `error` 판독: openclaw 트리거 턴 미응답(9/1 18:00:17), Python 체인은 18:12 완주 — 데이터 무손실.
- 발견: P1 알림이 어디에도 안 감(flush 슬롯 `alerts-digest`가 v0.2와 함께 폐기 → 적체 2건), KRX 태깅 129종목(KONEX·신형 코드) 매일 스킵, 텔레그램 토큰은 이미 설정됨(CURRENT 낡음).

## 2. ALERT-1 — 실행 보고 1통 + 미발화 감시 (`ed8de5b`, `c234c39`)
- `trading.run` 진입점: eod-v3·weekly-v3 종료 직후 텔레그램 1통(✅/⚠️ 부분 실패/❌ + stdout 최상위 ≤14줄 + 미발송 P1 꼬리). `RunStore`(`data/runs.sqlite`, started/finished append-only).
- `check-eod-v3`(18:30)·`check-weekly-v3`(토 10:00): 오늘 기록 없음 ❌ 미발화 / 시작만 ⏳ 미완료 즉시 발송, 완료면 침묵. openclaw cron 4잡 등록.
- 기각: openclaw delivery/텔레그램 채널 경유(트리거 턴 12분+ 점유, LLM 미응답=알림 누락).
- 적체 P1 2건은 dispatches에 `suppressed:fixed-64684cf` 행 append로 억제. 스모크 `sent:telegram`(프로젝트 첫 실발송, 봇 lucas_homeclaw_bot).

## 3. 페이퍼 = 매매 가이드 정합 (`c5d184d`, `dd67376`)
- 운영자 정정: 페이퍼는 100주 투자가 아니라 시작가 앵커 가이드. 원장 100단위 = 정규화(비중 100%). CLI·/paper에서 주수·투입액·총액 기준 수익률 제거(균등가중만).

## 4. EXEC-12 — 가이드 매도 예약: 실계좌 감시 + 조건주문 (`7266fce`~`5b6d6ab`)
- 실측: 토스 실보유 1~13주(평단 = 시작가) → CURRENT "각 100주" 오기 정정. 공식 스펙: 국내 지정가 timeInForce **DAY뿐** → 세션은 DAY 매일 재등록 제안 → 운영자 "예약 조건주문으로, 취소 후 재등록" → 헌법(금지 3·§6) 조건부 경로 동결을 **SELL·상방 감시가·지정가에 한해 해제**.
- `guide_orders.py`: 보유 스냅샷·변동 이벤트 → 우리 조건주문 정산(COMPLETED→filled·사다리 진행 / ORDERED / EXPIRED / 외부 종료) → 가이드 open 종목별 다음 매도선 1개(실보유×비중 내림, 0인 선 건너뜀, 호가단위 올림) → **수량 변경 시에만 취소·재등록**(2차 지시: 유지 판정 = 동일 계획·동일 보유·만료 2일 초과). 저널 `data/broker.sqlite` append-only. 모드 `GUIDE_ORDERS_MODE`·KILL 공유. 이익 보호·시한·veto 청산은 자동화 제외(P0+수동).
- `TossClient.place_sell_conditional`(범용 SELL, LIMIT 하드코딩)·`conditional_order`(상세) 추가. cron `guide-orders` 평일 08:40(5잡).
- **시작가 불변**(2차 지시): `paper rebase`는 `--correction <사유>` 정정 전용으로 봉인.
- **live 전환**(운영자 14:57, dry-run 관찰 생략): 장중 첫 실등록 5건(에이스침대·원림·대한약품·신세계I&C 각 1주, 아이퀘스트 7주 — 80%선, 만료 09-09) 토스 WATCHING 일치, 2회차 "유지 5·쓰기 0", cron 경로 수동 실행 + 텔레그램 보고 OK. 추가 매수(4→5주) 시 변경 재계획 실측.

## 5. 웹 재구성 (`5fca8bc`, `178bb10`)
- /paper: 표 1 가이드(매수 상한 숫자만·매도선/다음 매도선 %·정리 금액만) + 표 2 실계좌·예약(모드는 제목 옆 한 번, 상태 신규/유지/거부/없음/가이드 밖). "부터·가능·dry-run" 문구 폐기.
- /picks: 원장(판정 종목 전부, 캡 무관, 코어 이탈·심사 탈락 배지) + 심사 대기 큐(미심사 코어 12·산업당 3, 순환) — 결함 재현: 대한약품(승인·매수)이 제약 캡 4위로 잘려 페이지에서 사라짐 → 해소. 자동 심사·페이퍼 등록·대시보드 동일 목록.

## 6. 지시 에코백(7/14 규칙)
- 감시 슬롯 시각: 제안 18:20/09:50 → **18:30/10:00**(12분 체인 여유).
- EXEC-12 미답 항목은 권장안으로: 수량 내림 · 주문별 veto 창 없음 · 이익 보호/시한/veto 청산은 P0+수동.
- live 전환 시 세션이 지켜보는 상태에서 첫 실등록 1회 실행(08:40 무인 첫 실주문 회피).

## 7. 검증
- mypy 208파일 clean · pytest 전체 통과(신규 테스트: ALERT-1 12건, EXEC-12 13건, /paper·/picks 열 구성).
- 실측: 토스 조건주문 OPEN 5건 = 저널 sent 5건, 유지 판정 live 2회 확인, 텔레그램 실발송 3통.

## 8. 미결(다음 세션)
- 9/3 08:40 첫 자동 live 실행 — 장전 조건주문 접수 여부(거부 시 슬롯 이동). 9/5 weekly-v3 첫 자동 심사(큐 12 기준).
- EXEC-12 잔여: 다음 선 1개 vs 남은 전 선 동시 등록 · 실체결↔페이퍼 원장 정합 · 조건주문 만료 상한 실측(7일 통과).
- /picks 큐 산업 비례 캡·체류 aging — 순환 1~2주 관찰 후.
- KRX 태깅 129종목(KONEX·신형 코드) 영구 스킵 박제(PROPOSALS).
- 신규 통과 17종이 금융(보험·증권)에 몰림 — COLLECT-6 지배주주 PBR 전환 효과 추적 필요.
