# 2026-07-14~15 — 가드 전면 보수 + live 첫 실사이클 (EXEC-8~11, SEL-4)

> 운영자 "주먹구구식으로 하면 못 쓴다" 지적에서 출발한 이틀짜리 전면 보수 세션.
> 사고 조사 → 가드 일괄 감사 → 설계 확정(운영자 결정 6건) → 구현 → live 실검증까지.

## 운영자 결정 (전부 OPEN_QUESTIONS 🟢 박제)

- **SEL-4(+2차)**: 가격 회복 조건은 계획 명시·등급형(`prev_day_high_recovery`)으로 — 이진
  reclaim 폐지(신규 arm/abort 사용=폐기), 전일 고가 기준선을 진짜 T-1(KIS 일자별)로 교정,
  R5에 당일 고가·종가 이격 주입(도달 불가능한 임계 방지).
- **EXEC-8**: 재진입 정책(max_entries 1|2, 하드스탑 후 금지·쿨다운 30분·체감 50%) +
  진입 밴드(코드 산출 ∩ R5 조임 — 붕괴·소진·R:R 가드 3종 통합, C4·C5 해소) + 손절 플로어
  max(2틱,1%). 분할 OCO(익절1 브로커 상주)는 "동일 종목 복수 조건주문" 실측 게이트.
- **EXEC-9**: 시간손절 자동 집행 — 도래일 14:30~14:50 창(15시 직전 단타 회피).
  브래킷 만료 = 시간손절 +1거래일.
- **EXEC-10**: 진입 평가 09:10부터 + 09~10시 폴링 60초. 급등(+5%↑) 차단, 급락(저점 -5%↓)은
  낙폭 40% 회복 후 진입(기준=저점×0.6). 최소 배분 min(25%, max(15%, 50%/n)), n=감시+보유.
- **EXEC-11**: 저잔고(가용<계좌 30%) 모드 배분=계좌 15%. 교체 문턱 Δ(가중 수익률)≥1.5%p,
  미달=당일 폐기(재시도·트림 생략).
- **운영**: EXEC_ACCOUNT_KRW 500만→300만(게이트웨이 재기동 동반). 청산 큐 CLI
  (`python -m trading.liquidate`) 신설 — EXEC-1 잔여 "임의 청산 자동화 없음" 해소.

## 가드 일괄 감사(7/14) → 수정 현황

- **A묶음(돈 경로) 완료**: A1 브래킷 원자성(무방비=P0+박제·원복), A2 부분 체결 잔여 취소,
  A6 브래킷 생존 대조(sync_brackets), A7 미체결 매수 정리. 레그/트림 선박제(이중 매도 차단).
- **B묶음 완료**: ExecStore 쿼리 mode 필터 일괄, WatchStore armed@mode 분리, 레짐 P0 일자
  dedup, 전일고가 일자 캐시(프로세스 로컬·성공값만).
- **잔여(다음 세션)**: A5(armed↔집행 비원자성), C2(분모의 가드 차단 초안 오염),
  C3(단일 타깃 pct 무시), D1(UNKNOWN 레짐 진입 정책).

## 토스 API 실측 확정 3건 (전부 실사고로 적발)

1. **OCO first=익절(높은 감시가)/second=손절** — 반대로 보내던 어댑터 400. live 첫 브래킷
   등록 실패(~3분 무방비)로 적발.
2. **conditional_orders GET은 status=OPEN|CLOSED 필수**, 응답 키 `conditionalOrders`
   — A6 파서가 영영 판정 보류였던 것 해소.
3. **취소(DELETE) 응답 빈 본문** — json 파싱 크래시로 "취소 실패(보호 잔존)" 오판(실제론
   취소됨 = A1 역전). PSK 편입·콜마 청산 두 실사고로 적발, `_call` 빈 본문 허용으로 수정.
   + holdings() 아이템 스키마 핀(symbol/quantity/averagePurchasePrice/…).

## live 첫 실사이클 (테스트 매수 → 익절 → 청산)

- 7/14 운영자 지시 테스트 매수: 뉴파워 1주 @9,650 · 콜마 11주 @107,700 (수동 예외 — 밴드
  밖·RISK_OFF 중임을 명시하고 집행). 표준 경로(reconcile→OCO→포지션) 검증 완료.
- **뉴파워: 7/15 개장 OCO 익절 10,000 체결 — 시스템 첫 자동 익절(+3.6%)**. A6가 감지.
- 콜마: 청산 큐 → 구코드 빈 본문 버그로 취소만 되고 매도 불발 → 수동 집행 @104,300.
- 7/15 아침 결함 실측: 09:01 개장 직후 진입 + 각 1주 더스트 배분(동적 비례×소액 계좌)
  → EXEC-10/11로 해소. 수동 증량 편입 2회(PSK 4주→5주 OCO, S-Oil 3주→4주 OCO).
- 마감 상태: 보유 PSK 5주 @198,360·S-Oil 4주 @133,125(브래킷 WATCHING), 가용 ~130만.

## 재산출 사건 (7/14 밤)

reclaim 폐지 후 R5 재산출: 1차에서 entry_band "확장 금지 폐기"가 정상 산출 3건을 죽임 →
**교집합 절삭**으로 완화(넓히기는 여전히 불가). 재산출 결과 6건 전부 이격 3% 이내에서만
완전 회복 요구, 이격 +9.2% S-Oil은 가격 조건 자체를 제거 — 이격 주입이 의도대로 작동.
수동 재산출은 `trading.run synth-playbooks` 경로 필수(모듈 직접 실행 시 자동 승인 누락).

## 검증

- pytest 전체(신규 ~30 테스트: 밴드·재진입·시간손절·청산 큐·모멘텀·배분·mode 격리·캐시)
  + mypy strict 0 (135 files). 커밋 21건, main 19bac0e 푸시.
- 실거동: 브래킷 등록·익절 체결·수동 편입·청산까지 브로커 대조로 확인.

## 후속 (다음 세션 후보)

1. 감사 잔여 A5·C2·C3·D1.
2. **수동 매수 자동 편입**(A6가 보유 수량↔브래킷 수량 불일치 감지 시 자동 확장) — PROPOSALS P-13.
3. 분할 OCO 실측 게이트(동일 종목 복수 조건주문 허용 여부 — live 보유 중 확인 가능).
4. `drill.py --audit` — 이틀간 감시기 다수 재기동 후 무인 사이클 점검.
5. 08:50 select-am 장전 판정의 실질 가치 검토(지표 무의미 시간대).
