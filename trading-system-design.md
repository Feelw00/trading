# 트레이딩 리서치·집행 시스템 설계서

버전 0.2 (2026-06-08) · 대상: 단일 운영자(Lucas) · 기반: OpenClaw 스케줄러 프레임워크
변경: 운영 헌장 명문화, 스윙 시계 확정(생업 제약), R5.5 신설, 흐름 역학 조건문, 알림·보고 체계, 레짐 모니터, 청산 우선 자동화

---

## 0. 운영 헌장

1. 우리는 투자 심리를 따르지 않는다. 측정된 조건부 분포 위에서, 비용 차감 후 기대값이 양수이고, 무효화 조건이 관측 가능한 규칙만을 따른다.
2. 규칙은 100% 준수한다. 규칙의 수정은 장이 닫혀 있을 때 평가 루프(R7)를 통해서만 한다. 포지션 보유 중의 규칙 변경은 규칙의 부재와 같다.
3. 군중 심리는 규칙의 재료(엣지의 원천)이고 운영자 자신의 심리는 규칙이 가두어야 할 대상이다. 어느 쪽도 조종석에 앉지 않는다.

## 1. 목적·전제·설계 철학

이 시스템은 예측 기계가 아니라 **조건문 공장 + 규율 집행기**다. 검증된 교훈: 시장의 지배 변수는 예측되지 않으며, 수익은 예측의 질이 아니라 "트리거 미충족 시 진입하지 않는다"는 조건문 구조에서 나온다. 최종 출력물은 전망 보고서가 아니라 장 마감 후 확정되는 조건부 주문 초안, 알림, 그리고 근거의 박제다.

**운영 전제 (시스템 제약으로 강제):**

- 운영자는 생업이 있어 장중 부재가 기본 상태다. 시스템은 운영자의 실시간 개입 없이 하루를 완주할 수 있어야 한다.
- 매매 시계는 일~주 단위 스윙(최대 월 단위)이다. 초단기 회전은 범위 외. 시간 손절의 단위는 분이 아니라 거래일이다.
- 운영자의 자기 진단(하락 중 매수 경향, 장중 즉흥 판단)을 제약으로 인코딩한다: 주문 설계는 장 마감 후에만 생성되고, 장중의 운영자에게는 판단권이 없다. 장중 부재는 약점이 아니라 이 제약의 물리적 구현이다.

세 가지 불변 원칙: 숫자는 코드가 수집하고 텍스트만 LLM이 처리한다. 무효화 조건 없는 분석은 스키마 검증에서 자동 반려한다. 자동 매매(진입)는 평가 루프가 2개월 이상의 데이터를 확보하기 전까지 금지하되, 청산 자동화는 1순위로 앞당긴다(§6).

## 2. 전체 아키텍처

```
[Collectors]─▶[Fact Store]─▶[검증 R1]─▶[정형화 R2]─▶[페르소나 R3]─▶[적대 R4]─▶[합성 R5]─▶[보고 R6]
 (코드)        (as_of+출처)              (GPT-5.5)     (GPT-5.5 ×3)   (claude -p)  (claude -p)   │
      └──▶[이벤트 감시기(상시, 코드)]──▶[알림 체계 §8]                                          │
                  [R5.5 아침 플레이북 선택기 (08:50, 코드)]      [평가 R7 (주간, claude -p)] ◀──┘
```

### 모델 라우팅

| 작업 성격 | 엔진 | 근거 |
|---|---|---|
| 정량 수집, 이벤트 감시, R5.5 선택기 | 순수 코드 (LLM 없음) | 환각 차단, 지연 0, 비용 0 수렴 |
| 뉴스 분류·요약·이벤트 추출, 페르소나 분석 | GPT-5.5 API | 고빈도·저단가, 스키마 강제 출력 |
| 적대 토론, 합성·주문 초안, 주간 평가, 수집기 코드 유지보수 | Claude Code `-p` (headless) | 장문 컨텍스트·파일시스템·멀티스텝의 저빈도 무거운 작업 |

Claude Code headless 호출 옵션·출력 포맷은 공식 문서 기준: https://docs.claude.com/en/docs/claude-code/overview

```bash
claude -p "$(cat rounds/r4_adversarial_prompt.md)" \
  --output-format json > artifacts/$(date +%F)/r4_result.json
```

비용 가드: GPT-5.5 라운드별 토큰 상한, claude -p 작업은 일 2~3회 배치 제한. 라우팅은 OpenClaw의 라우터 패턴 재사용, 기준은 "작업의 구조적 성격" 고정.

## 3. 라운드 파이프라인

라운드 간 전달은 파일/DB 레코드로만 한다(프롬프트 체이닝 금지, 전 산출물 감사 가능).

| 라운드 | 이름 | 엔진 | 실행 (KST) | 핵심 출력 |
|---|---|---|---|---|
| R0 | 수집 | 코드 | 상시(소스별) | FactRecord |
| R1 | 신선도·정합성 검증 | 코드 | R0 직후 | 플래그 부착 FactRecord |
| R2 | 정형화 | GPT-5.5 | 06:00 / 16:00 | EventRecord, BinaryState |
| R3 | 페르소나 분석 | GPT-5.5 ×3 | 06:30 / 16:30 | ThesisRecord |
| R4 | 적대 라운드 | claude -p | 17:00 | 생존 논제 + 기각 사유 |
| R5 | 합성·플레이북·주문 초안 | claude -p | 20:30 | PlaybookSet, OrderDraft, 체크리스트 |
| R5.5 | 아침 플레이북 선택기 | 코드 | 08:50 | 플레이북 활성/비활성 (기본값: 비거래) |
| R6 | 보고 | 정적 렌더링 | 06:50 / 21:00 | 모닝 브리핑 / 저녁 결재 보고 |
| R7 | 평가·캘리브레이션 + 레짐 모니터 | claude -p | 토 10:00 | ScoreRecord, 레짐 지표, 프롬프트 개정안 |

### R0 — 수집 (결정론적)
LLM 비개입. 멱등성, 지수 백오프, 레이트리밋 공통 구현. 필수 필드 `as_of`, `fetched_at`, `source`. 소스는 부록 A.

### R1 — 신선도·정합성 검증
신선도 초과 시 `stale` 플래그(폐기하지 않되, R5는 stale 입력으로 주문 초안 생성 불가 — 하드 게이트). 핵심 지표(환율, 지수)는 이중 소스 임계 괴리 시 `conflict` 플래그 + 알림, 사람이 해소할 때까지 의사결정 제외. (실패 사례 박제: 캐시 선물 지표 오독, 8,851억 vs 53조 수급 오보.)

### R2 — 정형화 (GPT-5.5)
뉴스·공시 → EventRecord. 진행형 바이너리(예: 미·이란 협상)는 상태 머신 `{사안, 상태 enum, 근거, 다음 관측 포인트}`로만 추적. 형용사 금지.

### R3 — 페르소나 분석 (GPT-5.5, 입력 격리 병렬)

| 페르소나 | 입력 슬라이스 | 담당 질문 |
|---|---|---|
| 수급 | 투자자별 매매동향, 신용잔고, 예탁금, 레버리지 ETF 설정액, 공매도 잔고, 개인 강도 프록시 | 누가 사고 팔며, 강제 매물이 남았는가 |
| 사이클 | DRAM/NAND 고정·현물가, 캐펙스 가이던스, 컨센서스 변화율 | 이익의 2차 미분이 어디를 향하는가 |
| 매크로 | 환율(역내·NDF), 유가, 금리, 바이너리 상태, 당국 개입 시그널 | 외생 변수가 수급 루프를 강화하는가 차단하는가 |

출력은 ThesisRecord 스키마 강제. `invalidation` 누락·관측 불가 조건은 자동 반려(1회 재생성, 재실패 시 폐기). 시계(horizon)는 거래일 단위, 스윙 기본값 3~15일.

### R4 — 적대 라운드 (claude -p)
임무는 논제 살해 시도: 신선도 공격, 교란변수, 역사적 반례, 수단-논제 불일치, 컨센서스 할인, 타이밍-메커니즘 정합성. 각 페르소나의 최근 성적표(R7)를 입력받아 성적 나쁜 페르소나를 더 가혹하게 공격. 출력 `{생존(수정안 포함), 기각, 사유}`.

### R5 — 합성·플레이북·주문 초안 (claude -p, 20:30 고정 — 장중 실행 금지)
생존 논제를 시나리오 트리로 합성하고 **사전 승인 플레이북 묶음(PlaybookSet)**을 만든다. 플레이북의 역할 분리:

- 밤(R5, 내러티브 언어): 어느 종목의, 어느 방향 플레이북을 승인할지 결정.
- 아침(R5.5, 흐름 역학 언어): 승인된 플레이북의 발동 여부를 미시구조 지표로만 판정.

09~10시 구간의 가격은 f(가치)가 아니라 f(주문 흐름)이므로, 플레이북 발동 조건문에는 가치·내러티브 변수를 금지하고 흐름 변수만 허용한다: 갭 크기, 프리마켓 거래량, 호가 잔량 불균형, 체결강도, 동시호가 예상체결가 궤적, 거래량 클라이맥스, 신저가 갱신 실패. 역추세 플레이북은 "과도하다"는 논리 신호가 아니라 소진의 물리 신호 확인 후에만 발동 가능하도록 작성한다. 손절·트리거 레벨에서 "논리적 지지선"은 금지하되 "심리적 합의 레벨"(라운드 넘버, 전저점·전고점)은 허용 — 개인 비중 높은 레짐에서 자기실현적으로 작동하는 변수다.

OrderDraft는 §6 규율 파라미터를 스키마 강제. 출력에 익일 체크리스트와 저녁 결재 보고용 요약 포함.

### R5.5 — 아침 플레이북 선택기 (08:50, 결정론적, LLM 금지)
NXT 프리마켓 가격·거래량, 간밤 미국 마감, 환율 개장가를 읽어 PlaybookSet 중 조건 일치 항목을 활성화하고 해당 예약 주문을 arm한다. 가장 흔한 출력은 "오늘 해당 없음, 비거래"여야 정상이다 — 갭 전략의 엣지는 매일 여는 게 아니라 분포가 쏠린 날(서킷브레이커 익일 플러시 등)만 여는 데서 나온다. 08~10시 경로에 새로운 판단은 없다: 분석은 전날 20:30에 끝났고 아침은 패턴 매칭과 집행뿐이다.

### R6 — 보고 (§8 상세)

### R7 — 평가·캘리브레이션 + 레짐 모니터 (주간, claude -p)
채점: 페르소나별 적중률(트리거 발동 후 시계 내 방향 일치), 확신도 캘리브레이션, **운영자 준수율**(무효화 발동 시 청산 여부, 주문 초안 대비 집행 편차 — 수익률보다 선행 KPI), 적대 라운드 기각 정확도.

레짐 모니터: 시초 1시간 변동성, 개인 강도 프록시(시초 거래대금의 일중 비중, 레버리지 ETF 설정액, 신용잔고)를 추적. 백테스트 분포는 레짐 조건부로만 유효하다(개인 급증 레짐의 비정상성) — 레짐 지표가 식으면 플레이북 활성화 기준을 자동 보수화한다. 엣지의 사망 감지가 엣지의 발견보다 중요하다.

프롬프트 개정은 자동 적용 금지, 운영자 승인 후 버전 태깅.

## 4. 데이터 계약

```jsonc
// FactRecord — R0
{ "id": "krx.flows.foreign.2026-06-08", "metric": "kospi_foreign_net_buy_krw",
  "value": -1234500000000, "as_of": "2026-06-08T18:00:00+09:00",
  "fetched_at": "2026-06-08T18:05:11+09:00", "source": "krx_data_api", "flags": [] }

// EventRecord — R2
{ "id": "evt.2026-06-08.0142", "type": "policy|earnings|geopolitics|corp_action|flow_anomaly",
  "entities": ["SK하이닉스"], "summary_1line": "...", "binary_ref": "us_iran_mou",
  "evidence": ["FactRecord.id", "url"], "market_scope": ["KR.semis"] }

// ThesisRecord — R3 (무효화 필수)
{ "id": "thesis.2026-06-08.supply.01", "persona": "supply|cycle|macro",
  "thesis": "반대매매 소진 후 스윙 반등", "direction": "long|short|flat",
  "instrument_class": "KR.semis.large",
  "trigger": "익일 시초 갭다운 후 30분 내 저점 미이탈 + 거래량 클라이맥스",
  "invalidation": "플러시 저점 종가 이탈 또는 외인 순매도 가속(전일比 +50%)",
  "horizon_days": 5, "confidence": 0.55, "evidence": ["FactRecord.id"] }

// Playbook — R5 (발동 조건은 흐름 변수만)
{ "id": "pb.2026-06-09.hynix.flush_long", "thesis_ref": "thesis.2026-06-08.supply.01",
  "arm_conditions": { "gap_pct": "<-3.0", "premkt_volume_rank": "<=20" },
  "abort_conditions": { "new_low_after": "09:30" },
  "order_draft_ref": "order.2026-06-09.hynix.long", "default": "inactive" }

// OrderDraft — R5 (규율 파라미터 내장)
{ "id": "order.2026-06-09.hynix.long", "symbol": "000660", "side": "buy",
  "tranches": [
    {"label": "impatience_fee", "pct_of_plan": 20, "order_type": "limit"},
    {"label": "flush",          "pct_of_plan": 50, "order_type": "limit"},
    {"label": "confirmation",   "pct_of_plan": 30, "condition": "prev_day_high_reclaim"} ],
  "total_size_cap": "0.5 * normal_unit",
  "stop": {"type": "conditional_order_at_broker", "level": null},   // 청산 자동화 필수 (§6)
  "time_stop_days": 5, "created_when_market": "closed",
  "status": "draft|approved|armed|executed|expired" }
```

저장소: PostgreSQL + 파일 스토어. 레코드는 append-only.

## 5. 스케줄

시장 시간 인지형(휴장일·서머타임 반영). 장중(09:00–15:30) LLM 라운드 전면 휴면 — 정량 수집과 이벤트 감시만 가동.

| KST | 작업 |
|---|---|
| 06:00 | 미국 마감 수집 → R2 → R3 → 06:50 모닝 브리핑 |
| 08:50 | R5.5 플레이북 선택, 예약 주문 arm |
| 09:00–15:30 | R0 상시 + 이벤트 감시기. LLM·운영자 판단 없음 |
| 15:45 / 18:00 | KRX 잠정/확정 수급 수집 |
| 20:00 | NXT 애프터마켓 마감 수집 |
| 20:30 | R3(석간) → R4 → R5 |
| 21:00 | 저녁 결재 보고 발송 (§8) |
| 토 10:00 | R7 |

## 6. 집행·규율 레이어 — 청산 우선 자동화

운영자가 장중 부재이므로 **모든 포지션의 손절은 의지가 아니라 증권사 조건부 주문이어야 한다.** 자동화 우선순위를 뒤집는다: 진입 자동화는 Phase 4 조건부지만, 청산(스탑) 자동화는 Phase 1부터 필수다 — 증권사 스탑로스/조건부 주문 기능 또는 Open API의 청산 전용 연동(시장가 진입 영구 비활성, 청산 주문만 허용). 자리를 비우는 스윙 트레이더의 무효화 미준수는 성격 문제가 아니라 구조 문제이고, 구조 문제는 구조로 푼다.

진입 워크플로: 21:00 저녁 보고에서 OrderDraft 검토·승인(`approved`) → 예약 주문 입력(수동, 의도된 마찰) → 08:50 R5.5가 조건 일치 시 arm. 장중에 떠오른 아이디어는 당일 집행 불가, "장중 충동" 태그로 저널 기록만 허용(다음 R5 입력으로는 가능 — 하룻밤 냉각 강제).

3트랜치 기본 구조: 급함 요금 20%(진정제), 메인 50%(지정가), 확인 30%(가격 상승으로만 충족되는 조건 — 전일 고가 회복, 외인 순매수 전환 등; 하락 중 대량 매수를 구조적으로 차단). 변동성 레짐 감지 시 총량 상한 자동 절반. 가격 손절과 시간 손절(거래일 단위) 모두 필수.

## 7. 평가 루프 — §3 R7 참조. 채점 없는 멀티 에이전트는 가장 정확한 에이전트가 아니라 가장 유창한 에이전트를 신뢰하게 만든다.

## 8. 알림·보고 체계

원칙: **모든 알림은 사전 정의된 행동에 매핑된다.** "시장이 움직였다" 류의 행동 없는 알림은 금지 — 알림 피로를 만들고 즉흥 매매를 유도한다. 알림 페이로드는 `{무엇이, 어느 규칙을, 사전 약속된 행동은, 기한은}` 4요소 고정.

| 등급 | 채널 | 조건 | 매핑 행동 |
|---|---|---|---|
| P0 즉시 푸시 | 메신저 즉시 | 보유 종목 무효화 조건 발동·스탑 체결, 서킷브레이커/사이드카, 환율 임계(설정값) 돌파, 바이너리 상태 전이, 보유 종목 주요 공시 | 사전 약속된 단일 행동 확인(대부분 "자동 청산 체결 확인"으로 충분해야 함) |
| P1 묶음(점심·마감) | 메신저 다이제스트 | 주문 레벨 ±1% 접근, 외인 수급 전환 징후, 플레이북 arm/발동 | 저녁 보고에서 검토 |
| P2 정보 | 보고서 포함만 | 그 외 전부 | 없음 |

이벤트 트리거는 알림과 체크리스트 갱신까지만 수행하고 신규 주문 초안을 생성하지 않는다 — 패닉 장중에 시스템이 운영자에게 매매 아이디어를 주입하는 것 자체가 규율 위반.

보고 2종 (각 5분 내 독해 분량 강제):

- **06:50 모닝 브리핑(읽기 전용)**: 간밤 미국·환율·유가 3줄, 오늘 arm된 플레이북과 예약 주문 상태, 오늘의 관측 체크리스트, 모든 수치에 as_of 표기. 대부분의 날의 결론은 "오늘 할 일 없음"이어야 정상.
- **21:00 저녁 결재 보고(의사결정 문서)**: 오늘 계획 대비 집행 편차, 보유 포지션의 무효화 조건 잔여 거리, 수급 확정치, R4 생존/기각 요약, 내일 OrderDraft 승인 요청 목록. 운영자의 하루 중 유일한 판단 시점.

## 9. 운영·장애 대응

수집기 파손은 신선도 게이트가 감지(stale 알림 = 점검 신호), 수정은 claude -p 표준 작업(스키마 테스트 통과가 머지 조건). LLM 스키마 위반은 1회 재시도 후 폐기+알림. API 장애 시 R5는 "초안 갱신 불가, 전일 초안 유지/폐기 선택" 알림. as_of 없는 숫자는 출력 금지. 라운드별 일일 토큰 예산, 초과 시 강제 요약 모드.

## 10. 로드맵

**Phase 1 — 수직 슬라이스 (2~3주).** 반도체(삼성전자·SK하이닉스 / SOX) + 환율·유가·바이너리. R0~R6 + 알림 P0/P1 + **증권사 조건부 청산 주문 연동**. 완료 기준: 6/2~6/8 주간 리플레이가 수동 결론("조건 미충족 → 노 포지션 → 익일 플러시 대기")을 재현 + 모닝/저녁 보고가 5분 분량으로 생성.

**Phase 2 — 평가 루프 (2주).** R7, 저널 채점, 캘리브레이션 대시보드, 레짐 모니터. 이후 1개월 이상 저널 모드.

**Phase 2.5 — 갭 분포 백테스트.** 대상 유니버스의 갭 크기 × 변동성 레짐 조건부 지속/회귀 분포, NXT 프리마켓의 본장 예측력 검증, 거래 비용(현행 거래세율·수수료 확인 후 반영) 차감 기대값. 양수 확인 전까지 R5.5 플레이북은 "서킷브레이커 익일 플러시" 단일 유형으로 제한.

**Phase 3 — 섹터 확장.** 방산 → AI/플랫폼 → 의약. 기존 섹터 적중률 안정화가 전제.

**Phase 4 — 진입 반자동화 (조건부).** Open API(한국투자증권 KIS Developers, 키움 OpenAPI+ 등)로 OrderDraft 지정가 등록 자동화. 전제: R7 데이터 2개월 + 운영자 준수율 90% + 시장가 영구 비활성 + 전송 전 수동 확인 유지.

## 부록 A — 수집 소스 (Phase 1)

| 데이터 | 소스 | 주기 |
|---|---|---|
| KRX 시세·투자자별 매매동향 | KRX 정보데이터시스템 | 장중 주기 + 15:45/18:00 |
| NXT 프리·애프터마켓 시세·거래량 | 증권사 API/NXT 데이터 | 세션 중 |
| 신용융자·예탁금·개인 강도 프록시 | 금융투자협회, 파생 계산 | 일 1회(T+2 유의) |
| 환율(역내)·금리 / 역외 NDF | 한국은행 ECOS, 서울외국환중개 / 가용 소스 | 장중 / 일 2회 |
| 미국 시세(SOX·지수·주요 종목), 유가 | 시세 API | 해당 세션 |
| DRAM/NAND 고정·현물가 | 시장조사기관 공표치 | 주/월 |
| 공시 / 뉴스 | DART OpenAPI / RSS·키워드 폴링 | 이벤트 / 15분 |

## 부록 B — 페르소나 프롬프트 계약(공통 골격)

필수 포함: 입력 슬라이스 정의(외부 데이터 추론 금지), ThesisRecord 스키마와 무효화 관측 가능성 요건, 최근 자기 성적표, 금지 조항(타 페르소나 결론 인용 금지, 검증 불가 조건문 금지, 확신도-근거 비례). 적대 페르소나에는 표준 공격 벡터 체크리스트(신선도, 교란변수, 역사적 반례, 수단-논제 정합성, 컨센서스 할인, 타이밍-메커니즘 정합성) 내장.
